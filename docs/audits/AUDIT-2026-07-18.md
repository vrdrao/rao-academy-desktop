# SYSTEM HEALTH AUDIT — 2026-07-18, at commit `5eb6297` (read-only; nothing changed, committed, or pushed)

Produced in-session against BRIEF-HEALTH-CHECK.md (one-time system health audit).
Archived verbatim per BRIEF-CLOSEOUT-2026-07-18.md.

## 1. TREE — PASS

```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

The only file present beyond the tracked tree was `BRIEF-HEALTH-CHECK.md` itself, deleted per its own instruction before the checks ran. Nothing else untracked, nothing modified.

## 2. ENGINE — PASS

```
__version: "rao-master-18"
```

Exactly as expected.

## 3. GUARDS — PASS (full `npm test`, exit code 0)

The complete chain ran once end-to-end for this audit and exited 0 — every stage green. Directly observed output, by stage:

```
Line-ending guard: checked 104 lesson files.  PASS — all 104 files use LF
✓ all files in lessons/ and tools/ are tracked
✓ all lesson basenames are unique, no Windows copies
✓ all lessons are content-only (no embedded CSS/JS/fonts)
scope-css: all comment/selector invariants hold          (10/10 ok)
STRUCTURAL: all 6 cases pass — malformed authoring is rejected loudly.
FIREWALL: DEPENDENCY / RUNTIME / MUTATION / SOURCE-DIFF — 4 passed, 0 failed
HINT LEAK: 2765 rungs checked — PASS, all clean
HINT COVERAGE: 2707 questions — PASS, all have at least one hint
────────────────────────────────────────────────────────────
2722 questions built · 2722 rendered · 2722 grade correctly
2722 reject wrong answers · 0 blank figures · 0 JS errors
────────────────────────────────────────────────────────────
ENGINE rao-master-18 — SAFE TO SHIP ✅
verify-format: all 103 match the lesson format            (103 pairs; skips _-prefixed fixture by design)
verify-styles: selection styling is clean                 (ring, visible selection, keyboard ring, panel, mobile 380×800)
TOUCH: the calm card works with real touch events at 380px ✅
CALM CARD: all laws hold ✅   (incl. ANSWER-LEAK: 1,594 select questions, zero green, zero reveals on wrong)
ROBO: rig, ladder, silence, stuck-child, touch — all hold ✅
```

Capture disclosure (not laundered): the audit chain's stdout was tail-captured, so three final banners (harness, verify-styles, verify-touch) were not visible in that run's saved output. The chain's exit 0 covers them, but to show **actual output** I re-ran exactly those three on the same unchanged tree immediately after — the banners quoted above are from those re-runs. The verify-format line is from this session's standalone run of the same tree.

## 4. TOTALS — PASS

Re-derived from disk by running every lesson through the engine's `build()` (not by grepping):

```
lessons: 104   questions (via build()): 2722
MATCH: 104 / 2722 as recorded
```

One finding surfaced by this method: **7 questions carry warn-level `BUILD_WARNING`s** (pre-existing, not new). The full table:

| # | File | Question | Exact warning |
|---|---|---|---|
| 1 | `lessons/incoming/Division_facts_to_10.html` | q1 | `auth-q1: unknown figure data-show="equal-groups"` |
| 2 | `lessons/incoming/Division_facts_to_10.html` | q2 | `auth-q2: unknown figure data-show="equal-groups"` |
| 3 | `lessons/incoming/number-patterns-word-problems-remix.html` | q2 | `auth-q2: unknown figure data-show="sequence"` |
| 4 | `lessons/incoming/number-patterns-word-problems-remix.html` | q7 | `auth-q7: unknown figure data-show="sequence"` |
| 5 | `lessons/incoming/number-patterns-word-problems-remix.html` | q12 | `auth-q12: unknown figure data-show="sequence"` |
| 6 | `lessons/incoming/number-patterns-word-problems-remix.html` | q19 | `auth-q19: unknown figure data-show="sequence"` |
| 7 | `lessons/incoming/number-patterns-word-problems-remix.html` | q24 | `auth-q24: unknown figure data-show="sequence"` |

Each was emitted as `{"level":"warn","code":"BUILD_WARNING","message":"..."}` in the question's `issues` array. All 7 questions still render, grade, and reject correctly in the harness — the warning means the author asked for a named figure (`equal-groups` / `sequence`) that the engine doesn't recognize, so whatever visual was intended there is silently absent. That's the same failure shape as the "blank space where a figure should be" class of bug, which is why it is flagged despite the green suite.

## 5. COMMIT PROVENANCE — PASS with 2 UNTRACED

All 30 commits, newest first:

| Commit | Traced to |
|---|---|
| `5eb6297` review set + index + manifest | `BRIEF-REVIEW-INDEX.md` — executed by Claude this session. **Caveat:** that brief mandated its own deletion and is in no durable record (not BRIEFS.md, not a handoff). Trace rests on the session's own attestation. |
| `aa140dd` HANDOFF-9 | Standing checkpoint rule (recorded in HANDOFF-6 §Process note) |
| `5b26bec` deploy-drop regen (18) | HANDOFF-9, its own section |
| `d5818b1` 7.7.3 keypad | HANDOFF-9 item 4 (7.7.x brief texts are **not** in BRIEFS.md — handoff-recorded only) |
| `27cc376` 7.7.2 wrong-feedback | HANDOFF-9 item 3 (same caveat) |
| `f7b372c` 7.7.1 clamps/review | HANDOFF-9 item 2 (same caveat) |
| `4c8cbcd` Brief 7.7 Robo | BRIEFS.md Brief 7.7 + HANDOFF-9 item 1 |
| `077db41` 7.7 Amendment 1 | BRIEFS.md Amendment 1 + HANDOFF-9 push range |
| `55a98b0` amend 7.7 | Staging of the BRIEFS.md amendment text itself |
| `cae228a` 7.7 prerequisites | Named by Brief 7.7's PRE-CHECK |
| `c906775` guided-solve-rebuilt-v1 | Brief 7.7 canonical source + `docs/GUIDED-SOLVE-REBUILD-NOTE.md` |
| `2515a4f` deploy-drop (17) + DEPLOY.md | **UNTRACED** — see below |
| `a0b372f` HANDOFF-8 | Checkpoint rule |
| `16f5c50` Brief 7.6.1 | BRIEFS.md 7.6.1 + HANDOFF-8 |
| `22be6f7` "commit" | Venkat's own commit (BRIEFS.md 7.6.1 text, +60 lines); explained in HANDOFF-8 |
| `905b264` HANDOFF-7 | Checkpoint rule |
| `09cc647` 7.6 rulings | Cited by BRIEFS.md 7.6.1 header + HANDOFF-7 |
| `219e156` rao-master-16 stamp | Brief 7.6 + HANDOFF-7 |
| `e43dd91` Brief 7.6 Calm Card | BRIEFS.md 7.6 |
| `9183ef3` stage v36 + spec | Brief 7.6 (md5-pinned reference) + HANDOFF-7 |
| `be3874e` HANDOFF-6 | Checkpoint rule |
| `f06d335` fixture codes + CODE REGISTRY | HANDOFF-6 milestone list |
| `533b13e` Brief 7.5 proof | BRIEFS.md 7.5 + HANDOFF-6 |
| `a9d05dd` type-coverage count fix | HANDOFF-6 (push base `a9d05dd..f06d335`) |
| `491a128` Brief 7.3 | BRIEFS.md 7.3 + STATUS.md "7.3 DONE" |
| `d9d74f6` 59 whyWrong templates | BRIEFS.md 7.4 scope + STATUS.md "7.4 DONE" |
| `fb22f81` gitignore settings.local.json | **UNTRACED** — see below |
| `4a3c595` anti-laundering rule (CLAUDE.md +7) | BRIEFS.md Brief 1 standing instruction ("Update CLAUDE.md with any new rule… commit locally") |
| `89ea5ee` resolve unexplained distractors | BRIEFS.md 7.4 scope + STATUS.md "7.4 DONE" |
| `7bbdeed` taxonomy + classifier + CRLF | BRIEFS.md 7.4 scope + STATUS.md "7.4 DONE" (names taxonomy and classifier) |

**UNTRACED (2):**
- **`2515a4f`** — "Add deploy-drop/ for tulipmath.com: rao-master-17 engine drop + DEPLOY.md". No brief in BRIEFS.md and no handoff authorizes its creation. STATUS.md's "Deploy state" section *records* it after the fact, and HANDOFF-9 builds on it, but a record is not an authorization.
- **`fb22f81`** — "Ignore local Claude Code settings (per-machine config)". No brief or handoff. Session memory records this as a direct instruction from Venkat ("never commit settings.local.json"), but that is not an on-disk repo record.

Both are almost certainly Venkat-directed in chat; flagged because the paper trail doesn't show it, and per the project's own rule the classifier doesn't get to call that benign.

## 6. DEPLOY DROP — PASS, 16/16

Every file md5- and size-compared against its repo source (`deploy-drop/<f>` vs `engine/<f>`, fonts included):

```
MATCH  preview-engine.js   4b6f84dd5eaee2bb316ad0b3a8ccf424  (196,698 bytes)
MATCH  rao.css             31a3ab64ac26098bcecf296863e27cad  (89,837)
MATCH  rao-card.css        7b7ef624505cfd99aac26bb5459b38ac  (9,748)
MATCH  rao-card.js         8af4da4bcc4f972dd702265fe29ed09a  (27,472)
MATCH  solution-renderer.js 0a17636d35a482cf82ebeaf65e65fa1c (15,207)
MATCH  robo.js             f137b5ff4f2774abfef1fe3ab4d96aba  (51,393)
MATCH  robo.css            6b7336bf9f7a7ff872874e716631d715  (51,183)
MATCH  fonts.css           84d7b35f3a39e180225c94059c921f68  (9,756)
MATCH  8/8 fonts/*.woff2
TOTAL: 16 match, 0 mismatch
```

The 8 hashes also match DEPLOY.md's printed fingerprint table exactly, so the doc a deployer would follow is truthful.

## 7. UNVERIFIED

- **The authorizing brief for `5eb6297`** (the review-set commit) cannot be independently verified from disk — `BRIEF-REVIEW-INDEX.md` was deleted per its own instruction. Claude executed it in the same session, so this is first-hand attestation, not inference — but a future auditor will hit the same wall, and the same will be true of the health-check brief. If briefs are to be auditable later, they need to land in a durable record before deletion. *(Addressed the same day: the docs/briefs/ archive rule, BRIEF-CLOSEOUT-2026-07-18.)*
- **The audit `npm test` run's early-stage output** was not retained in full (tail-capture); covered by exit code 0 plus targeted same-tree re-runs of the three uncaptured banners, disclosed in §3.
- **The two UNTRACED commits** in §5 — believed to be Venkat's instructions, but not provable from the repo at audit time. *(Resolved the same day — see below.)*
- Everything else in this report was verified by actually running it in-session.

**Verdict: healthy.** Clean tree, correct engine, full suite green, totals match to the question, deploy drop byte-faithful. The flags worth attention: the 7 `data-show` figure warnings (§4), the 2 untraced commits (§5), and the brief-deletion audit gap (§7).

---

## Untraced commits — resolved

- **`2515a4f`** (deploy-drop for tulipmath.com): directed by Venkat in chat; corroborated by STATUS.md "Deploy state" and HANDOFF-9. **Ruled AUTHORIZED retroactively, 2026-07-18.**
- **`fb22f81`** (gitignore settings.local.json): Venkat's standing instruction, previously recorded only in session memory. **Ruled AUTHORIZED retroactively, 2026-07-18.**
