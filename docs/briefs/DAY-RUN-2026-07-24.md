# DAY-RUN-2026-07-24

Chat-authored chained run. Venkat IS present today — stop-gates behave
normally (stop and ask; he will paste your question to chat).

## WHAT TO RUN, IN ORDER

1. `BRIEF-SHOWN-ANSWER-1.md` (repo root) — execute exactly, including its own
   full `npm test` and local commit.
2. Then, without waiting for a human if no gate fired:
   `BRIEF-CELEBRATE-1.md` (repo root) — execute exactly, including its own
   full suite and local commit. The file MUST contain the section
   "SOUNDS (RULED by Venkat 2026-07-24)". **If that section is absent, STOP
   and say so — the file is stale; do not run the sound-less version.**

## RULES

- Stop-gates in both briefs are ACTIVE — stop and ask when one fires.
- One combined close-out at the end covering BOTH briefs: per brief — phases,
  failing-then-passing guard evidence, deviations with reasons, commit hash,
  diff stat. Then the enumerated push checklist:
  `git log --oneline origin/main..HEAD`.
- **NEVER push.** No side-quests; anything noticed outside scope gets one
  line in the close-out instead.
