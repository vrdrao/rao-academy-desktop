# ISSUES — the authoritative record of known issues

This file is the **authoritative record of known issues** for the project.
Handoffs reference item numbers here; they do **not** restate issue detail.
Issues found during review get a row **at the time they are found**, not at
handoff time.

**Contents are asserted, not measured.** Unlike `STATE.md` (which is regenerated
from the repo by tooling), nothing in this file is verified by any script — no
script can know that a figure is unanswerable. **A row may be stale.** Trust it
the way you trust a colleague's note, not the way you trust a passing test.

## The three laws

1. **Keyed by question ID wherever one exists.** Every question carries a
   permanent opaque `id: q********`. That is the join key: an issue logged in July
   stays findable in November even if the lesson is renamed or archived. Where no
   single id applies (engine/infra), the id column is `—`.
2. **Rows are never deleted, only closed.** The same append-only law as
   `docs/question-ids.json`. Closing means setting `status`, filling `closed` with
   a date and `resolution` with how it was resolved, and **leaving the row in
   place.** Deleting a closed row destroys the record of *why* something is the way
   it is — which is how a superseded tile-sizing scheme resurfaced twice and was
   nearly re-applied. **Item numbers are permanent** and are never reused or
   renumbered.
3. **An issue may be closed as `not-a-defect`.** When investigation shows the
   report was mistaken, the row stays, closed, with the reason — so nobody
   re-opens it and burns another twenty minutes. Item 41 is the worked example.

## Column meanings

| Column | Meaning |
|---|---|
| `#` | permanent item number — never reused, never renumbered |
| `grade` | 4, 3, or `all` for engine/infra issues |
| `question id` | `q********` where one exists, else `—` |
| `file` | source file, or `—` |
| `issue` | one line, specific enough to act on without context |
| `lane` | `E` engine · `P` pattern sweep · `L` lesson-specific · `D` working as designed · `infra` · `content` |
| `severity` | `correctness` · `layout` · `polish` · `infra` |
| `status` | `open` · `ruled` · `in progress` · `closed` · `not-a-defect` · `parked` |
| `opened` | date the issue was logged |
| `closed` | date closed, or `—` |
| `resolution` | how it was resolved, or `—` |

**`severity: correctness` means a child is shown something wrong or unanswerable.**
It ranks above everything else regardless of effort.

## Issues

| # | grade | question id | file | issue | lane | severity | status | opened | closed | resolution |
|---|---|---|---|---|---|---|---|---|---|---|
| 41 | 4 | `q86pfikqr` | area-and-perimeter-word-problems | Reported: L-shape unsolvable, correct answer 12 not among options | L | correctness | not-a-defect | 2026-07-20 | 2026-07-20 | BRIEF-SOURCE-READ: source has 6 labels for 6 sides, figure closes (left 4 = 2+2, bottom 6 = 4+2). Shoelace on authored coordinates = 20 sq cm, which IS among options [24,20,16,10]. The "12" came from mis-decomposing the notch width as 2 instead of bottom width 6. Reviewer misread, not a defect |
| 44 | 4 | `qe4c5gevv` | bar_graphs_remix | Categorize renders no data — "sort by how many" with no quantities. Data ABSENT from source, not failing to render; quantities exist only in `explain:`, shown after answering | content | correctness | closed | 2026-07-20 | 2026-07-21 | BRIEF-BATCH-1: body now renders a single-series bar chart (reused this lesson's existing `pv-graph` inline-SVG mechanism; no new renderer). Quantities taken verbatim from the existing `explain:` text — Lion 30, Zebra 10, Monkey 40, Seal 15; no numbers invented. Every sort is determinable before answering. New `verify-answerable.js` hard-gates this id |
| 44b | 4 | `qszpxymg7` | bar_graphs_remix | Categorize renders no data — sort teams by points, no points given. Same shape as `qe4c5gevv`; authoring defect, one file, two questions | content | correctness | closed | 2026-07-20 | 2026-07-21 | BRIEF-BATCH-1: body now renders a single-series bar chart (same reused `pv-graph` mechanism). Quantities taken verbatim from the existing `explain:` text — Reds 70, Blues 30, Golds 60, Greens 50; no numbers invented. Greens 50 sits exactly on the "50 or fewer" gridline. New `verify-answerable.js` hard-gates this id |
| 45 | all | — | — | No guard proves a question is *answerable* from what is rendered. Spec written by BRIEF-SOURCE-READ Task D; not built. **First half built (BRIEF-BATCH-1): `tools/verify-answerable.js` implements Assertion 2 (categorize must present a data source) — hard-gates the two known cases, flags the rest for review. Assertion 1 (select answer selectable) and fill-blanks/order/etc. remain unbuilt.** BRIEF-PUBLISH-1 adds `verify-no-dead-end.js` — a sibling robustness guard proving every question resolves (no infinite retry) — which strengthens the "child always reaches a resolution" family but does NOT complete the answerability assertions above | infra | correctness | open | 2026-07-20 | — | — |
| 49 | 4 | — | Multiply_two-digit_by_two-digit_word_problems__1to1 | Reported: only lesson missing its enrichment pass. MEASURED WIDER: it is indistinguishable from the `_remix` baseline (both 0% whyWrong, 0% solution). The full §13 pack exists in only **19 of 103 lessons** — 84 lessons sit at 0% whyWrong. Not one lesson; a corpus-wide gap. **NOT closed by BRIEF-PUBLISH-1:** Task A (Item 50) makes the missing content *survivable* (a second wrong reveals the answer instead of looping) but does not author the §13 pack; the enrichment gap stands | content | correctness | open | 2026-07-20 | — | — |
| 48 | all | `qukz2ne4j`, `qwy5e27zq`, `qnh5ry3b4`, `q55c5764u`, `qyz6te24b`, `qpg3sxjip` | box_multiplication_remix | Partial-product box should be drag-to-cell, not multi-select; multi-select does not verify which product belongs in which cell. Authoring format needed before G3 conversion begins | E | correctness | closed | 2026-07-20 | 2026-07-21 | BRIEF-AREAMODEL-1. **Re-ruled 2026-07-21 (Venkat): TYPED CELLS, not drag-to-cell** — reasoning recorded so it is not re-litigated: (1) reuses a proven mechanism — `areaModel` now emits `<input class="blank-input">` cells graded by the existing fill-blanks path (`fill.serialize` sorts every blank by `data-blank`); drag-to-cell would need a new SVG drop system; (2) typing tests more than dragging — a child who types 3600 has multiplied; dragging four tiles into four cells leaks (three right makes the fourth free); (3) it mirrors paper. Mechanism: **Option 2 (HTML grid)**, not SVG `<foreignObject>` — the foreignObject probe passed on Chromium but its named risk (mobile Safari) is untestable in-harness, and HTML `<td><input>` is the engine's proven typed-grid pattern (divisionTable/colmath/vertical). The 6 questions are now `fill-blanks` with `mode="type" sums="hide"`; keys DERIVED from each decomposition (verified in Python, cell-sums == top×side), not lifted from the old options. Guarded by `verify-area-model.js` (grades-by-position incl. right-number-wrong-cell → wrong; no regression on filled/blank; visibility at 390×844 & 360×780), all sabotage-proved. Format documented in `WORD_TO_AUTHORING_INSTRUCTIONS.md`. **Measured: 25 `area-model` questions corpus-wide (not 6) — see Item 58** |
| 50 | all | — | — | On ~86% of questions a second wrong answer re-offers "Try again" indefinitely — no walkthrough, no cap. A child can be stuck in a loop. **Root cause measured: there is nothing to show** — see Item 49 | E | correctness | closed | 2026-07-20 | 2026-07-21 | BRIEF-PUBLISH-1 Task A. **Real root cause: the `&& canWalk()` conjunct** on the two-wrong cap (`rao-card.js`), NOT the absence of a cap — the cap existed but only fired where a walkthrough existed. A1 measured **449/2668 questions have a `solution:` (16.8%), 2,219 do NOT (83.2%)** — the dead-end path was the normal path. Fix: `commitCap()` caps EVERY question — `openWalkthrough()` where a solution exists (unchanged), else `showAnswer()`, a new fallback that reveals the answer via `revealCorrect()`, shows one warm line ("Here's the answer — you've got this!"), records the **new outcome string `shown-answer`** (distinct from `solved-with-help`), and offers "Next question →". No third attempt, no fabricated pedagogy (Venkat's ruling). Guarded by `tools/verify-no-dead-end.js` (Guard 1 no-dead-end + Guard 2 walkthrough-unchanged, both sabotage-proved; visibility checked at 390×844 and 360×780) |
| 55 | all | — | tools / npm test | `whyWrong` coverage is **ungated** — `npm test` runs `verify-content-guards.js --hint-leak-only`. Nothing has ever enforced whyWrong, which is how 84/103 lessons reached 0% unnoticed | infra | correctness | closed | 2026-07-20 | 2026-07-21 | BRIEF-BATCH-1: `tools/verify-whywrong-floor.js` added — a **ratcheting** per-lesson floor recorded in `docs/whywrong-floor.json` (current state: 280/2668 = 10.5%). Wired into `npm test` and `test:fast`. FAILs if any lesson's whyWrong-covered count drops below its floor; never auto-raises the floor (raising is a human act in a brief). Does NOT force 100% — a permanently-red suite gets bypassed; it forbids regression. Proved to fail by sabotage (removed one whyWrong → FAIL naming lesson/floor/saw), restored byte-identical → PASS |
| 53 | 4, 3 | — | — | MCQ preferred over fill-blanks corpus-wide — a fourth grader taps more easily than types. Fill-blanks acceptable occasionally for variety; single-select is the default. **Applies to Grade 3 authoring from question one.** **EXCEPTION (Venkat, 2026-07-21, BRIEF-AREAMODEL-1): the area-model partial-product box (Item 48) is typed cells, NOT a violation — Item 53 is about answer *selection* (tap one of four beats typing it), but the box's content IS "which product belongs in which cell," which cannot be a tap without drag-to-cell or an unusable 12-option grid. Not re-litigated.** | P | layout | ruled | 2026-07-20 | — | AMENDED 2026-07-22: Venkat's ruling this session is STRONGER — "in fourth grade i don't want kids to do a lot of fill in the blanks". Record as a Grade 4 tightening. Does NOT reach #60's Grade 3 prohibition, and does not void #48's typed-cell exception. Venkat has explicitly DECLINED an automated corpus survey of fill-blank density — he is finding them lesson by lesson himself. Do not propose the survey again unless he asks. |
| 54 | 4 | — | — | Corpus-wide scan needed: count fill-blanks questions across all 103 lessons, per lesson, and how many admit conversion to single-select. Gates the Item 53 conversion work | infra | infra | closed | 2026-07-20 | 2026-07-21 | BRIEF-BATCH-1: `docs/audits/FILLBLANKS-CENSUS.md` — **599 fill-blanks (22.5% of 2,668)**; 359 single-blank / 240 multi-blank; 131 carry a structural `layout:`; **330 admit clean conversion** (rule: 1 blank + numeric + no `layout:`). READ-ONLY census; no conversions made. Scopes Item 53, does not execute it |
| 56 | 4 | — | interpret-remainders | 29 questions currently fill-blanks; candidate for conversion under Item 53. First lesson identified by review | P | layout | open | 2026-07-20 | — | — |
| 57 | all | — | .githooks/pre-push | Proposed (BRIEF-CONTENT-1 Task G): reduce pre-push to a fast integrity check because the full suite allegedly runs at BOTH commit and push (~25 min each, duplicated) | infra | infra | not-a-defect | 2026-07-21 | 2026-07-21 | BRIEF-CONTENT-1 Task G STOPPED at G1 (measure-first): the premise is false. BRIEF-PRECOMMIT-SPEED (2026-07-18) already split the gates — `pre-commit` runs `npm run test:fast` (~1–2 min), `pre-push` runs the full `npm test` (~12–15 min) exactly ONCE. There is no duplicate full-suite run to remove. Reducing pre-push would delete the ONLY full-suite gate (the browser suites run solely at push), a safety regression. Neither hook changed |
| 29 | 4 | — | — | Multi-expression prompts run horizontally; must stack | E | layout | open | 2026-07-20 | — | BRIEF-PUBLISH-1 B1: the "multi-expression prompt" signature is not cleanly detectable statically (needs a visual pass to define which prompts qualify), and the stacking fix is a shared-CSS/engine change affecting all 2,668 questions. **Deferred to the same focused styling pass as Items 39 and 52.** Stays open |
| 58 | 4 | — | box_multiplication_remix | `area-model` figures: Item 48 named 6, but the corpus actually has **25** (all in `box_multiplication_remix`) — the 6 blank-mode multi-selects (now converted to typed cells, Item 48) plus **19 `mode="filled"` stimulus figures** used as worked-example pictures inside other questions | E | infra | closed | 2026-07-21 | 2026-07-21 | BRIEF-AREAMODEL-1 §1: measured 25 area-model figures corpus-wide. The 19 filled ones are unchanged pictures (proved byte-identical old-vs-new engine build across all 25 questions) and are NOT affected by Item 48. Recorded so the "6" number is not mistaken for the full population. Also records the Task-A mechanism decision: **Option 2 (HTML grid) was chosen over SVG `<foreignObject>`** — the geometry was reproduced in an HTML `<table>` (headers, cells, optional row-total boxes), matching the proven divisionTable pattern; no SVG geometry survives in `mode="type"` |
| 33 | 4 | — | — | `₹` rendered unconditionally on non-money column sums | content | polish | not-a-defect | 2026-07-20 | 2026-07-21 | BRIEF-CONTENT-1 Task A (docs/audits/CONTENT-CENSUS-33-38.md): measured every `₹` in the corpus — **201 questions across 23 files, all genuinely about money. NOT-MONEY = 0, AMBIGUOUS = 0.** The 58 with no prose money-word are bare currency ops (`₹613 × 8`, `₹361 rounded to nearest ₹100`). Only 3 column/vertical/stack questions carry `₹`, all money. The reported defect does not manifest; nothing stripped, nothing to fix |
| 38 | 4 | — | — | `·` middot separator reads as multiply | content | polish | closed | 2026-07-20 | 2026-07-21 | BRIEF-CONTENT-1: census found **328 `·` occurrences (172 lines, 17 files), all PROSE-SEPARATOR; METADATA = 0, MULTIPLY = 0, AMBIGUOUS = 0.** Task B replaced every ` · ` → `, ` in lesson content (corpus stayed 2,668). Task C changed the expression keypad in preview-engine.js (` · ` → ` × `, insert + backspace, arithmetic unchanged) — cosmetic, nothing grades on `·` (P3 verified). Two new guards: `verify-no-middot.js` (lessons) and `verify-engine-no-middot.js` (engine), both proved by sabotage |
| 39 | 4 | — | — | Comparison figures stack vertically; must sit side by side. Chat recommends side-by-side desktop/tablet, stacked on phone | E | layout | open | 2026-07-20 | — | BRIEF-PUBLISH-1 B1: measured **13 questions** carry 2+ top-level figures (find_the_probability, Count_vertices…, identify-three-dimensional-figures, Multiply_1x2_remix ×3, perimeter_missing_side_remix, simple-fractions-parts-of-a-group ×2, Understanding_probability_remix ×2, make_predictions). The side-by-side fix is a shared responsive-CSS change (figures are un-wrapped siblings in `.qbody`) affecting all questions. **Deferred to a focused styling pass** (Venkat's ruling 2026-07-21). Stays open |
| 40 | 4 | `q86pfikqr` | area-and-perimeter-word-problems | Dimension labels collide with figure boundary. **Separate from Item 41 and still live** — the figure is correct, its labelling is cosmetically wrong | L | layout | closed | 2026-07-20 | 2026-07-21 | BRIEF-PUBLISH-1: measured (getBBox) two labels poking ~3px into the L-shape stroke — left "4 cm" (right edge 57 vs shape left 54) and lower-right "2 cm" (left edge 267 vs shape right 270). Font renders at authored 19px (figure not auto-scaled), so this is positional, not size — `dim-label` counter-sizing (to 20px) would slightly worsen it. Fix: nudged the two labels outward (x 35→28 and x 289→297); both now clear the boundary, verified by getBBox. The notch labels sit in the L's concave void (no stroke), which is correct |
| 42 | 4 | — | — | Options render full-width single column; should be 2×2 | E | layout | not-a-defect | 2026-07-20 | 2026-07-21 | BRIEF-PUBLISH-1 B1: measured `getComputedStyle(...).gridTemplateColumns` at tablet width (820px) across the corpus — **0 of 1,647 select questions render single-column** (with ≥3 options). `repeat(auto-fit, minmax(150px,1fr))` already yields 2+ columns. Single column at phone width (390px) is correct. The reported defect does not manifest; nothing changed |
| 46 | 4 | `q8nhv3ty3` | — | Y-axis interval too fine (5s to 100); sibling chart `qpwstmk82` is the correct pattern to copy | E | layout | closed | 2026-07-20 | 2026-07-21 | BRIEF-PUBLISH-1: measured exactly **1 question** with a fine 5-step y-axis (21 tick labels). Removed the 10 intermediate odd-multiple-of-5 gridline+label pairs, leaving a 10-step axis (0,10,…,100 = 11 labels) matching sibling `qpwstmk82`. Bars unchanged (same 168px/100 scale). Per-lesson figure edit |
| 47 | 4 | `qpwstmk82` | — | Chart legend clipped — "Cats"/"Dogs" lose final letter | E | layout | closed | 2026-07-20 | 2026-07-21 | BRIEF-PUBLISH-1: legend text sat at x=412 (`Cats`/`Dogs` extend to ~432) but the `viewBox` width was 430, so SVG overflow-clip cut the final letter. Fix: widened `viewBox` `0 0 430 232` → `0 0 448 232` so the legend fits; all coords unchanged, chart scales to fit its container. Single-question figure edit |
| 51 | 4 | `q3wypsawf` | area-and-perimeter-word-problems | Q19 expected `Answer: A`, observed B — Ctrl+F5 check carried unchecked across four handoffs | L | correctness | not-a-defect | 2026-07-20 | 2026-07-21 | BRIEF-CONTENT-1: Venkat reviewed `q3wypsawf` (Q19 of 25) directly. Both gardens are 24 sq m; A is 12×2 with perimeter 2(12+2)=28 m; B is 6×4 with perimeter 2(6+4)=20 m. **A needs more fence, so `Answer: A` in the file is correct.** The observed "B" was a reviewer misread — same shape as Item 41. `question id` column set to `q3wypsawf` (was `—`; an id now exists) |
| 52 | all | — | — | Kids should not be asked to scroll unless necessary — Check button must be reachable at 390×844 without scrolling. Candidate guard, not yet built | infra | layout | open | 2026-07-20 | — | BRIEF-PUBLISH-1 Task C: measured **64 of 2,642 questions (2.4%)** with the Check button >844px from the card top at 390×844 (worst are figure-heavy: identify-three-dimensional-figures up to 1,611px, box_multiplication, frequency_tables). **Many are legitimately tall content** (large 3D-figure SVGs), so the fix is a sticky/reachable Check button, NOT shortening content — a shared-CSS/engine change affecting all questions. **Deferred to a focused styling pass** (Venkat's ruling 2026-07-21). Stays open |
| 31 | 4 | — | — | "HINT 1" shown when only one hint exists | E | polish | parked | 2026-07-20 | — | — |
| 32 | 4 | — | — | Categorize gives no verdict and no per-tile feedback | E | polish | parked | 2026-07-20 | — | — |
| 37 | 4 | — | — | Answer line shows bare values, unmappable to blanks | content | polish | parked | 2026-07-20 | — | — |
| 34 | all | — | tools/verify-reset.js | Addresses frames by hard-coded index | infra | infra | parked | 2026-07-20 | — | — |
| 36 | all | — | deploy-drop/ | Stale frozen copy | infra | infra | parked | 2026-07-20 | — | — |
| 59 | all | — | `tools/make-review.js` | Review pages are written to a flat, grade-blind `review/` directory with no existence check — `make-review.js:53` hard-codes OUT_DIR to `review/`, `:329` writes OUT_DIR + basename, `:330` is `fs.writeFileSync` with no guard. `argv[3]` overrides the filename only, never the directory. `review/` is TRACKED (104 committed pages), so a collision silently destroys committed content and surfaces only as an after-the-fact `git diff`. Present collisions: 0. Prospective: 5 (`complete_the_division_table`, `estimate_sums__word_problems`, `multiply_numbers_ending_in_zeroes`, `properties_of_multiplication`, `round_money_amounts`). The obvious fix makes things worse: 12 code sites hard-code `review/` and walk `lessons/` only, so a `review-g3/` mirror would not crash — it would SILENTLY not cover Grade 3, trading a loud rare failure for a silent permanent one. | infra | correctness | open | 2026-07-21 | — | Ruled 2026-07-21 (Venkat): do NOT build today. Fires only if a Grade 3 lesson takes one of the 5 slugs, and we control the naming. Free insurance in force: name every Grade 3 lesson output file to avoid all 5 slugs. Build as Phase 2's opening move, bundled with grade-aware guards — same root cause (every tool assumes one grade), one job. |
| 60 | 3 | — | — | Grade 3 is tap-or-drag only. No typed input from a third grader. `fill-blanks` is prohibited from lesson 4 onward. Lessons 1–3 are a recorded exception (lesson 1 ships 1; lessons 2 and 3 may carry up to 2 each). | P | layout | ruled | 2026-07-21 | — | Supersedes Item 53 for Grade 3 — Item 53 made MCQ a preference; for Grade 3 it is a prohibition. Item 53 stands unchanged for Grade 4. Item 48's typed-cell exception does NOT carry to Grade 3. Consequence: the approved 14/6/3/4/3 type mix is VOID from lesson 4 — what replaces the 6 fill-blanks is a Phase 2 spec ruling, not a per-lesson improvisation. Guard in Phase 2: any `lessons-g3/` file with `type: fill-blanks` FAILS, exception list = lessons 1–3. |
| 61 | all | — | `docs/WORD_TO_AUTHORING_INSTRUCTIONS.md` | The authoring doc documents a format we have abandoned, and it is the file the team will author from. MEASURED 2026-07-21: zero occurrences of `solution` in the entire document. `hint` is documented at `:99` as "optional — one-line learner hint", and Golden Rule 4 at `:145` mandates `description`, `hint` and the explanation be "short and kid-friendly (one line each)". The spec lives only in `CLAUDE.md` §13. The doc does not merely omit `solution:` — it instructs the opposite of the ruling on hints. | infra | correctness | open | 2026-07-21 | — | Not fixed here. Must land before any team member authors from this document, and must carry Items 60, 62, 63, 64, 65, 66. |
| 62 | 3 | — | — | Hint ladders are 1 rung by default, 2 maximum. Three or more is a defect. A second rung must earn its place — restating the first in different words does not qualify. Reasoning (Venkat, 2026-07-21): a Grade 3 child has neither the time nor the patience for that much text, and these questions are not complex enough to support three genuine escalating stages. MEASURED: lesson 1 shipped 30/30 at 3 rungs. | P | layout | ruled | 2026-07-21 | — | No engine change needed — `rao-card.js:55` already accepts a 1–3 rung array, so a 2-rung ceiling is stricter than the engine and is a content rule. Guard in Phase 2: `lessons-g3/` file with more than 2 rungs FAILS. Applied to lesson 1 by BRIEF-G3-RULINGS-2 Task B. |
| 63 | all | — | `rao-card.js` | When the solution panel opens, accumulated hint bubbles must be hidden. Venkat's screenshot (2026-07-21, lesson 1 Q3) showed hint bubbles and the solution stacked on one screen — unnecessary scrolling, too much text for a Grade 3 reader. MEASURED: `openWalkthrough()` (`rao-card.js:291`) already hides the hint button at `:299` but leaves the accumulated bubble chat container rendered. | E | layout | closed | 2026-07-21 | 2026-07-21 | CLOSED by BRIEF-G3-ENGINE-1 (Change 3): `openWalkthrough()` (and the new `revealSolution()`) now remove the accumulated hint/whyWrong bubble container (`chat.remove()`) at the point the hint button is hidden, so the solution panel stands alone. Law 4 comment amended in the same change to name the walkthrough-open as the one sanctioned exception to HELP ACCUMULATES. No-repaint held: the chat lives OUTSIDE `.qbody`, so the question DOM is untouched. Guarded by `verify-solpanel.js` assertion 3 (bubbles present before open, ABSENT after; qbody is the SAME node — no repaint), proved RED pre-fix and by sabotage; `verify-calm.js` f re-pointed to sample hint chips before the open. |
| 64 | all | — | — | No hint rung may contain the answer, at ANY position — not merely the final rung. A hint points at the METHOD and stops short of the arithmetic. | P | correctness | ruled | 2026-07-21 | — | The rule stands. Lesson 1 already satisfies it — measured 2026-07-21, 0 leaking rungs across all 30 questions, all 3 rungs each, using per-type matching (numeric whole-token, text substring, order sequence, categorize bin-mapping). The evidence originally cited for this item was a MISREAD and is corrected here: chat reported lesson 1 Q3 (6 × 9 = 54) as having two rungs stating the answer. Those two strings were the Q3 whyWrong message for the 45 distractor (`:101`) and the Q3 explain line (`:97`) — not hint rungs. Q3's real rungs are the ten-trick ladder and contain no 54. Why the misread was possible, and why it will recur: the engine renders whyWrong into the same bubble stream as hints with the same chip numbering (`rao-card.js:118` — "the next Hint n chip number, whyWrong and forward rungs share it"), so a screenshot showing "HINT 1" may be showing whyWrong content. See Item 66. BRIEF-G3-ENRICH-1's final-rung leak check was therefore NOT defective — it reported 0 because the ladders are clean. Guard in Phase 2: every rung, every position. |
| 65 | all | — | `rao-card.js` | On a correct answer the child must have nothing to read by default. MEASURED: `celebrate()` (`rao-card.js:366`) scans the `solution:` for the first `takeaway` block and renders it in a `.cc-take` panel ("The idea to keep") 550 ms after the chime. Venkat's objection (2026-07-21, lesson 1 Q4, 5 × 8 = 40): the takeaway is the abstract generalisation with the working stripped out, it lands on a child who just succeeded and wants to move on, and Grade 3 reading ability is limited. | E | layout | closed | 2026-07-21 | 2026-07-21 | CLOSED by BRIEF-G3-ENGINE-1 (Change 4): `celebrate()` no longer renders the `.cc-take` panel — nothing to read by default. `cc-hastake` IS still added (only where a solution exists) so the duplicate `.explain` stays sealed. Green paint + `cc-win` + sparks + chime unchanged (correct is still loud). A ghost "Show me the solution" is offered beside "Next question →" when a solution exists; the render was EXTRACTED into `renderSolutionPanel()` so the correct-path `revealSolution()` keeps outcome `correct` while `openWalkthrough()` records `solved-with-help`. Law 7 amended. Guarded by `verify-solpanel.js` assertions 4 (no `.cc-take`, `.explain` sealed, `cc-hastake` present, `.cc-win` present) and 5 (reveal renders, outcome stays `correct`); `verify-calm.js` g re-pointed to the four-condition form; cap-out path proved unchanged (Item 50). ORIGINAL RULING (kept): correct shows the FULL solution — steps, takeaway, verification — COLLAPSED behind a "See how"/"Show me the solution" tap. Not the takeaway line alone, not nothing. Reasoning: the objection was never to the solution existing on the correct path, but to a stripped abstract line sitting in the way of a child who wanted to move on; collapsing satisfies both — the default screen is green option + chime + "See how" + "Next question", and the full teaching is one tap away. TRAP — `rao-card.js:384` sets `qbody.classList.add("cc-hastake")` with the comment that the panel carries the teaching and hides the duplicate `.explain`. Suppress the takeaway naively and `.explain` REAPPEARS, swapping one block of text for another. `.explain` must stay suppressed. Second documented-law reversal: `rao-card.js:43` states CORRECT is the only loud moment (green option, cc-win, sparks, chime, takeaway panel, then Next question). Amend it in the same change. The fixture must also prove the cap-out path is UNCHANGED — still auto-opening the full walkthrough with no tap — because Item 50's fix lives there and must not become collateral damage. |
| 66 | all | — | `rao-card.js` | `whyWrong` messages are chip-labelled "Hint n" and share the hint numbering (`rao-card.js:118`). A child who answers wrong sees a bubble labelled "Hint 1" that names the correct answer — which `whyWrong` is designed to do, since explaining why 45 is wrong requires saying what right looks like. Calling it a hint is what caused both Venkat and chat to misread lesson 1 Q3 as a leaking hint ladder (see Item 64). | E | layout | closed | 2026-07-21 | 2026-07-21 | CLOSED by BRIEF-G3-ENGINE-1 (Change 2): a `whyWrong` message now renders with the chip "Not quite" (never "Hint n"), does NOT increment `hintNum` (the next hint the child asks for is still "Hint 1"), and carries the warning tint `.cc-msg-why` (created in `rao-card.css`, derived from `.cc-bub` geometry + the existing `.pv-why`/`.pv-fb.bad` warning tokens — no new hue). Law 5 amended to "TWO SEPARATE STREAMS". Guarded by `verify-solpanel.js` assertion 2 (chip "Not quite", then a hint still reads "Hint 1"); `verify-touch.js` and `verify-reset.js` re-pointed. |
| 67 | all | — | `rao-card.js` | The walkthrough button read "Walk me through it", which reads as an extension of the hint ladder — children were expected to mistake it for "hint 3". Ruling A (Venkat, 2026-07-21): rename to "Show me the solution". | E | layout | closed | 2026-07-21 | 2026-07-21 | CLOSED by BRIEF-G3-ENGINE-1 (Change 1): the button label is "Show me the solution" at the offer site (`rao-card.js`) and in the law-6 comment; a grep proves ZERO remaining occurrences of "Walk me through it" in `rao-card.js`. Guarded by `verify-solpanel.js` assertion 1 (new label present, old label absent) and by the re-pointed `verify-touch.js` / `verify-calm.js`, which each now assert the NEW string appears AND the OLD string appears nowhere in `rao-card.js` (two conditions where there was one). |
| 68 | 3 | — | — | Ruling C (Venkat, 2026-07-21): a `whyWrong` message names the MISCONCEPTION, never the answer. If tapping a wrong option tells the child the right answer, guessing becomes cheaper than thinking — the child should learn what was wrong and still have to find the answer. Binding on all Grade 3 lessons. | P | correctness | ruled | 2026-07-21 | — | Applied to lesson 1 by BRIEF-G3-LESSON1-REWRITE: 21 of 46 `whyWrong` messages rewritten to name what the wrong option IS (e.g. "54 is 6 × 9") and its relationship to the ask ("right family, wrong pair"), without stating the answer. VERIFIED: 0 messages contain their question's answer as a whole numeric token (1 naive substring — the "0" inside option label "90" on Q7 — is not a leak). The other 25 messages already complied and were left byte-identical; 8 bare "x" multiply signs were corrected to "×". Phase 2 guard: every `whyWrong` message, whole-token, must not contain its answer. |
| 69 | 3 | — | — | Grade 3 multiplication solutions are PRINTED FACTS, never a method. Venkat (2026-07-21): the step-by-step "split 8 into 4 + 4 and double" is the distributive property (Grade 5/6); a child who does not know 7 × 8 does not know "double 28" either. Print the times table up to the fact, and nothing else. | P | layout | ruled | 2026-07-21 | — | SUPERSEDES the 2026-07-20 ruling "solution blocks may run 2–3 steps, do not trim" FOR GRADE 3 (that ruling lives in `CLAUDE.md` §13 and was applied by BRIEF-G3-ENRICH-1; it was never a separate ISSUES item and it STANDS for Grade 4). Applied to lesson 1 by BRIEF-G3-LESSON1-REWRITE: all 30 `solution:` blocks replaced by five shapes — 11 one-table, 2 two-table (comparison), 4 table-with-gap (categorize), 8 facts lists, 5 rule blocks (×0/×1/×10) — via engine block types `table`/`facts`/`rule` (BRIEF-G3-ENGINE-1). The engine computes every product; no product is written in frontmatter. All `step`/`takeaway`/`verification` blocks deleted (0 remain). Phase 2 guard: no `lessons-g3/` solution may contain `type: step`/`takeaway`/`verification`. |
| 70 | all | — | `preview-engine.js` | The frontmatter parser does NOT parse the MULTI-LINE YAML form of a solution `table`/`facts` block — `build()` sets `tables`/`items` to "" for the nested-list form documented in BRIEF-G3-ENGINE-1 §Change 5. INLINE JSON parses (matching the working categorize `regions:` pattern). MEASURED 2026-07-21: a nested-YAML `tables:` yields `solution[0].tables === ""`; `tables: [{"factor":8,...}]` inline parses to an array and renders. | infra | correctness | open | 2026-07-21 | — | Worked around in lesson 1 (BRIEF-G3-LESSON1-REWRITE authors `tables:`/`items:` as inline JSON; `rule:` parses either way). NOT fixed at the engine. Also a fixture gap: `verify-solpanel.js` checked a frontmatter-authored table only for `.sol-walk` presence, not table CONTENT, so it did not catch this. Phase 2: either extend the parser to handle nested YAML lists in solution blocks, OR document inline JSON as the required form and strengthen the fixture to assert rendered table content from a frontmatter-authored question. |
| 71 | 3 | — | `lessons-g3/multiplication_facts_up_to_10.html` | Hint blocks stripped from all 30 questions — **cleared for reselection, not abolished** (Grade 3 default is no hint; Venkat names individual questions to receive hints back after visual review). Hints remain a supported engine feature. | content | polish | closed | 2026-07-21 | 2026-07-21 | BRIEF-G3-HINT-STRIP-1 §1: removed 30 `hint:` blocks (60 lines — one `hint:` + one rung each; all 30 were single-rung, structurally identical). `diff` proved ONLY hint lines changed (0 lines added, 0 non-hint lines removed). All protected fields byte-identical: 30 questions, mix 14 SS / 6 FB / 3 MS / 4 cat / 3 order, 46 whyWrong (code entries), 30 solution, 30 explain, 2 header middots. Post-strip: `build()`=30 with 0 empty markup and 0 build issues; self-grade 30/30. No commit — file is untracked staging. |
| 72 | 3 | — | `rao-card.js` | On a no-hint question the BRIEF-G3-HINT-STRIP-1 §4 target is: first wrong shows whyWrong + the solution **shown directly**. The engine instead **offers** the solution via a ghost "Show me the solution" button alongside a "Try again", and only auto-opens the walkthrough on the **second** wrong. Observed live (Playwright, stripped lesson) on single-select (has whyWrong), fill-blanks and categorize (no whyWrong): 1st wrong → button + "Try again"; 2nd wrong → walkthrough auto-opens. For the 13 no-whyWrong questions the first-wrong screen is two buttons and no words. | E | layout | open | 2026-07-21 | — | NOT fixed here — measure/strip brief; the engine change is a separate guard-first brief. Root: `walkOffered()` (`rao-card.js:185`) is true on the first wrong once `allHintsUsed()` (which returns true when `rungs` is null — no hint) → `feedbackRow()` (`:212`) renders the ghost offer button; `commitCap()` (`:404`) auto-opens only at `wrongCount>=2`. Pre-existing two-attempt design, surfaced (not caused) by the strip. Phase 2 engine decision: should a no-hint question reveal the solution directly on the first wrong. |
| 73 | all | — | `rao-card.js` | Dead-per-question hint markup after the strip: `card()` always emits `.pv-hintbox` (`rao-card.js:84`, `hidden`) and the `.pv-hint` button (`:85`) into every card regardless of hint presence; with no rungs, `wireCard` hides the button (`:540`, `display:none`) and the box stays `hidden` + empty. Verified live (Playwright): hint button `display:none`, hintbox `hidden`, NO empty hint bubble, NO orphan chip or zero-counter in any wrong/walkthrough state. | E | polish | not-a-defect | 2026-07-21 | 2026-07-21 | BRIEF-G3-HINT-STRIP-1 §0.4: pre-existing engine behaviour, unchanged by this strip and correctly suppressed — no orphan reaches the child. Logged so the always-emitted markup is not re-reported as a strip regression. §0.6 checked separately: removing the bubble chat when a solution opens does NOT conflict with the append-only law — the chat lives OUTSIDE `.qbody`, so the question DOM is never rebuilt (Item 63). No conflict found. |
| 74 | 4 | `qqt5mus4F` | — | Venn/categorize tile glyph escapes its chip on return-to-tray (lesson `qqt5mus4F` Q8). Letter renders outside its white chip after being dragged back to the tray. Triage E (engine-wide, all Venn/categorize questions). NIGHT. Note for whoever briefs this: measure liftGhost first — this may be a gap in the existing drag-ghost fix rather than a missing one. | E | layout | open | 2026-07-22 | — | — |
| 75 | 4 | `qgtig2x2q` | — | "Geometry engine not loaded." (lesson `qgtig2x2q` Q10). Question renders but the drawing canvas is replaced by an error string; the question cannot be answered. Cause unknown — engine may not exist, may not be loaded on this page, or may fail for this question type. First action is a read-only count: how many questions across lessons/ depend on the geometry engine, in which lessons, and does the engine file exist in the repo. Dead content in an approved, pushed grade. | E | correctness | closed | 2026-07-22 | 2026-07-22 | BRIEF-FIX-GEO-WIRING-1 (diagnosis REPORT-DIAG-CRIT-1). CONFIRMED root cause: `engine/geometry-engine.js` exists and defines `window.RaoGeo`, but `tools/make-review.js` never inlined it, so `window.RaoGeo` was undefined on every review page and all 31 construct questions hit the else-branch error at `preview-engine.js:2631`. Systemic, not one question. Fix = **Option B**: make-review.js now inlines geometry-engine.js ONLY into pages whose lesson source has a construct question (shared detector `sourceNeedsGeo`), loaded BEFORE the card renderer so RaoGeo exists at mount. 6 construct pages regenerated (~780 KB → ~1.75 MB each; +5.6 MB total, vs +98.7 MB had every page carried the 962 KB engine). Live-verified: RaoGeo defined, ZERO "Geometry engine not loaded", 31/31 construct questions render, and a geo-construct spot-check grades a correct rectangle TRUE (false→true). New guard `tools/verify-geo-wired.js` (wired into `npm test`; sabotage-proved FAIL→PASS both by breaking detection and by removing the wiring) asserts the wired-page set EQUALS the construct-lesson set via an INDEPENDENT `data-type="construct"` detector, so a future construct lesson cannot silently go dead again. **NOTE — no consuming app yet:** the review pipeline is the only render surface today; whatever system eventually renders these lessons MUST also load `engine/geometry-engine.js` (currently untracked in git — Venkat must `git add` it) or the 31 questions break there too. |
| 76 | 4 | `qnpwn98bv` | — | Categorize grading may be positional rather than identity-based (lesson `qnpwn98bv` Q16). Review answer string renders as "has, none, has, none", which is positional. Unverified whether the grader compares per-tile identity or tray position. If positional AND tiles shuffle, correct answers can be marked wrong. Read-only verification needed: (a) does the grader compare identity or position, (b) do tiles shuffle on load. Severity potentially HIGH. | D | correctness | not-a-defect | 2026-07-22 | 2026-07-22 | BRIEF-FIX-GEO-WIRING-1 (diagnosis REPORT-DIAG-CRIT-1). NOT A DEFECT — grading is identity-keyed, not positional. `categorize.serialize` sorts tiles by authored `data-idx` (`preview-engine.js:2554`) and `check()` compares index-by-index (`preview-engine.js:2838`); tiles never shuffle (no shuffle path anywhere in the engine or rao-card.js — the only `Math.random` uses are an inline-id prefix and confetti). Live-graded qnpwn98bv Q16 through the real engine: correct placement → TRUE; the SAME identity mapping with tile positions physically reversed and DOM-swapped → still TRUE (position is irrelevant); a wrong mapping → FALSE (grader discriminates). The "has, none, has, none" seen in review is the answer-LINE display of the frontmatter `answer` array, not a misgrade. Blast radius: 154 categorize questions across 68 files, 0 at risk. Row kept per law 3. |
| 77 | 4 | `qkqnqae7e` | — | Question phrasing (lesson `qkqnqae7e` Q2). Original: "When you add years and months, you regroup months into a year once you reach how many months?" Procedure-first phrasing; the actual question appears in the last four words; uses "regroup" inside the question testing that concept. RULED: rewrite to "How many months make one year?" Options unchanged (12 / 10 / 24 / 100). Note: the existing hint reads "How many months in one year?" and becomes redundant after the rewrite. | L | polish | open | 2026-07-22 | — | — |
| 78 | 4 | — | — | Procedure-first phrasing sweep. Item 77 may be a pattern, not a one-off. Read-only sweep across lessons/ for question prompts opening with a procedural clause ("When you...", "If you...", etc.) where the actual question appears only at the end. Report count and lesson distribution. | P | polish | open | 2026-07-22 | — | AMENDED 2026-07-22: #104 makes prompt redundancy a Venkat-ruled corpus-wide sweep. Different pattern, same sweep, probably one brief. Cross-reference #95, #99, #101, #103, #104, #105. |
| 79 | all | — | — | Button label: rename "Show me the solution" to "Show solution". Engine-wide, both grades. RULED. | E | polish | open | 2026-07-22 | — | AMENDED 2026-07-22 (two-click finding, also touches #80). Venkat verbatim: "When I click got it, It should just take me to the next question. what is the point of taking them to click on the next question button again?" Currently "Got it" dismisses the solution and returns the child to the card, where they must then click "Next question" — two clicks for one intention, on the FAILURE path. WEIGH BEFORE MERGING: "Got it" currently gives the child a beat back on the question card seeing the correct answer in context. If that beat matters, keep one button but RELABEL it "Next question" once the solution has been read, so it is honest about what it does. Chat leans toward merging — the solution panel already showed the answer highlighted green. Pairs with the PARKED Enter-to-check request below: same engine pass, same question about what buttons and keys do in each state. — PARKED, UNNUMBERED (Venkat) — Enter key checks a fill-blank. Venkat verbatim: "when I'm in a fill in the blank and I enter a value And press Enter on the keyboard, it should automatically mean check." Logged, not scheduled; give it a number when it is scheduled. FOUR SUB-DECISIONS UNRESOLVED: multi-blank behaviour (Enter advances to the next blank, checks only on the last — a round-scaffold has three); empty input (Enter does nothing, not submit blank and mark wrong); after-wrong state (button reads "Try again" — does Enter retry?); after-correct state (button reads "Next question" — Enter advancing lets a child do a whole lesson from the keyboard, but risks reflex-skipping the celebration; may need a short re-arm delay). ENGINE-WIDE, not fill-blank-only: once Enter works in one place children will try it after multiple-choice and after order tiles. Fill-blank-only is a DECISION, not a default. |
| 80 | all | — | — | Insufficient padding between the solution/options block and the buttons below it (Next question / Show solution). Needs an optimal value measured, not guessed. Engine-wide. | E | layout | open | 2026-07-22 | — | AMENDED 2026-07-22: bundle with the #79 two-click finding — "Got it" → "Next question" merge means one fewer click on the failure path; same button-area engine pass. See #79. |
| 81 | all | — | — | SOLUTION PANEL VERTICAL LAYOUT CONTRACT. The defect: solution panels grow downward and never sideways. They have no rule for using the width they are given, so they default to a single column and the child scrolls unnecessarily. Observed on a Grade 3 times-table panel (8 rows in roughly one third of the available desktop width, two thirds empty), but the defect is panel-level and applies to every solution shape in both grades, including shapes not yet built. Device context ruled: grades 1-5 are primarily mobile and tablet, shifting to laptop at higher grades. Scrolling is acceptable for genuinely long content; the goal is to eliminate scrolling that is an artefact of layout. Requires a written layout contract at panel level so every shape inherits it, not per-shape fixes. Read-only measurement pass is the next step. Severity HIGH — affects every grade, every solution, every device. | E | layout | closed | 2026-07-22 | 2026-07-22 | CLOSED by BRIEF-PANEL-LAYOUT-1, built to SOLUTION-PANEL-LAYOUT-CONTRACT-v1 (guard-first, 4 phases + human review gate). THE CONTRACT: content declares SEQUENCE (an ordered set of short self-contained items — columnises across via CSS grid auto-flow row, `cols = clamp(floor(avail/(longest+gutter)), 1, 4)`, across-order per R3, cap 4 per R1) or BLOCK (a single thing that loses meaning if broken — never reflowed, full width, scroll rail if it overflows). Undeclared → BLOCK fail-safe. IMPLEMENTATION: `engine/solution-renderer.js` marks a single table and a facts list `.sol-seq` (two-table `sol-tables-2` does NOT columnise internally — R7; step cards stay BLOCK — R8); new `layoutSequences()` sets `--sol-cols` from the PANEL width via `panelWidthFor()` which walks up to `.pv-solwrap`/`.sol-holder`/`.pv-card` — NOT the sequence's immediate container, which in a walkthrough bubble shrink-wraps to the one-column width (the Phase-4 failure Venkat caught: auth-q1 rendered 1 column / 7 rows in the `.cc-bub`); a narrow MutationObserver lays out sequences wherever they render (renderSolution AND walkthrough bubbles), without editing `.sol-walk`/`.cc-bub`/`wireWalkthrough`. `engine/rao-card.css`: `.sol-seq` grid (`--sol-cols` default 1 = today's behaviour) + per-item scroll rail capped at `--sol-itemmax`; `.sol-working` is BLOCK. HONEST COST (Ruling R6): `sol-working` is NOT itemised — 164 of 274 comma-bearing working strings in the corpus are prose a `", "` split would shatter (e.g. "1 kL is 1,000 L, and 842 falls short of 1,000."), and nothing in the content distinguishes a list from a sentence; so the Grade-4 `steps` phone overflow becomes a scroll rail, not a reflow. Itemisation deferred to a content brief (author `working` as a list across ~499 step questions), not abandoned. GUARD: `tools/verify-panel-layout.js` asserts A–I (columnise-to-4, across-order, not-column-count, longest-item-1-col, sol-working-BLOCK, no-mid-item-break, wide-item-rail, block-never-columnises, undeclared→block, panel-width-unchanged) in BOTH the renderSolution holder AND a real `.cc-bub` walkthrough bubble — 8 sabotages, each proved to fail its named assertion in both contexts. The bubble context exists BECAUSE the Phase-3 guard passed while the product broke (it only tested the full-width holder). Before/after in the bubble: times table 1 col → 4 cols, panel height −76% (−70% table-absent) at 758px; 2 cols at phone. Full `npm test` green (no existing guard weakened). Venkat verified auth-q1 in the browser (Phase 4): 4 columns, across order, correct highlight. Item 83 (row-window proposal) was parked pending this; the layout defect is now fixed by columnisation, not windowing. |
| 82 | 3 | — | — | Stale chip label "SOLUTION — STEP BY STEP" renders above Grade 3 printed-fact solution panels. Contradicts the Grade 3 ruling that solutions are printed facts, never a method. Label needs to be honest about what is on screen. | E | layout | open | 2026-07-22 | — | — |
| 83 | 3 | — | — | PARKED, NOT RULED. Proposal that solution panels show a window of rows around the answer (e.g. 3 rows: one above, target, one below, shifted at table edges) rather than the full table. Venkat initially ruled yes, then correctly identified that this solves a content question and does not address the layout defect in item 81. PARKED pending resolution of 81. Do not implement. If revisited, open questions are: window size (3 vs 5 rows, pattern visibility argument favours 5), behaviour on the table-with-absent shape (Q25 has two absences far apart in the table and may need two windows or a longer table), and whether the "THE TIMES TABLE" chip label remains honest when showing 3 rows. | P | layout | parked | 2026-07-22 | — | AMENDED 2026-07-22: #81 is now FIXED and verified, so #83 becomes rulable again. Its open questions were: window size (3 vs 5 rows), behaviour on the table-with-absent shape, and whether the "THE TIMES TABLE" chip stays honest at 3 rows. NOTE: columnisation (#81) may have removed the motivation entirely — a times table at 4 columns is 68px tall, so there may be nothing left to window. |
| 84 | 4 | `q4zmc89u3` | — | Numeric fill-blank REJECTS a correct answer typed with a comma separator. The prompt shows numbers with commas (33,276 + 18,097) but the grader accepts only the un-separated form (42613); a child who types the natural comma form is marked wrong. Confirmed live (q4zmc89u3 Q17). PATTERN-WIDE: every numeric fill-blank with an answer >= 1,000. Fix: normalise (strip commas/spaces) before compare so both forms grade correct; guard misplaced-comma false-accepts and count the affected population at fix time. | E | correctness | closed | 2026-07-22 | 2026-07-23 | CLOSED by BRIEF-RETRY-STATE-2 (G3) 2026-07-23. Engine check() fill-blanks branch strips VALID comma grouping before compare — Western /^\d{1,3}(,\d{3})+$/ AND Indian lakh /^\d{1,2}(,\d{2})+,\d{3}$/ (Venkat 2026-07-23: Indian children are taught lakh grouping and must never be marked wrong for writing a number the way school taught them). A misplaced comma matches neither pattern, is left intact, and stays wrong (sabotage-proved: 4,2613 and 426,13 both reject; no valid-vs-misplaced ambiguity found, so no STOP). Scoped to fill-blanks only; single-select still matches data-val verbatim. Population 121 numeric fill-blanks >= 1000. Display formatting UNCHANGED — whether to DISPLAY Indian grouping is a separate open product decision, not this brief. Guard tools/verify-retry-state.js G3 (proved failing then passing). |
| 85 | 4 | `q4zmc89u3` | — | Fill-blank input box too narrow for multi-digit numeric answers — a 5-digit answer is clipped, ~4 digits visible; the child cannot see the full number they typed. PATTERN-WIDE: every numeric fill-blank with a 4+ digit answer. OPEN at fix time: display-clip only, or does the narrow box truncate the graded value (would make it correctness). Fix: size the input to the expected digit count (known from the key) or auto-fit. | E | layout | closed | 2026-07-22 | 2026-07-23 | CLOSED by BRIEF-RETRY-STATE-2 (G4) 2026-07-23. WIDENED the box, font UNCHANGED (Venkat ruling — shrinking text to fit a container degrades legibility). rao.css base .blank-input 64->96px (fits the 6-digit widest corpus key at 1.4rem; measured client 92 >= scroll 92 at 390x844 and 360x780, not clipped). Round-scaffold operands ->84px and the RESULT box ->96px via .rsf-grid>.rsf-blank:last-of-type (the sum can be a digit wider than either operand, qm37aecdj); .rsf gets overflow-x:auto so three widened boxes never break a 360px card. Per-digit variants (vm/cm/lat/dt/am) keep their own narrower widths. Severity stayed layout — Phase 0 correctness gate proved the grader receives the FULL string. Guard verify-retry-state.js G4 (5- and 6-digit, both viewports). PRIOR NOTES: AMENDED 2026-07-22: the round-scaffold RESULT box needs MORE width than the operand boxes, because the sum can be a digit wider than either operand. Evidence `qm37aecdj`: boxes sized for 3000/7000 (4 digits) but the result holds 10000 (5 digits), touching both edges. Venkat verbatim: "The input boxes are a little too tight to contain the numbers. As you can see, ten thousand is barely fitting in. We have to make the wider, or we have to reduce the font size of the input box text." RULED: WIDEN, do not shrink the font — reducing text size to fit a container degrades legibility for a nine-year-old to solve a container problem. |

AMENDED 2026-07-22 (second amendment). The row's OPEN question — "display-clip only, or does the narrow box truncate the graded value (would make it correctness)" — is SETTLED as display-clip only. Evidence `qqwwhgsih` ("Round 28,262 to the nearest 100"): the child's typed 28262 renders sheared mid-glyph, but the answer line reads ✓ Answer: 28300 — the grader holds the full value. Severity stays `layout`; does NOT promote to `correctness`. No child has been graded wrong by this. TWO ADDITIONS: (1) a THIRD affected layout — the single-box "28,262 → ▢" round-arrow form, distinct from `q4zmc89u3` (plain fill-blank) and `qm37aecdj` (round-scaffold); three layouts, one defect, confirms the E lane. (2) Recorded severity understates it: the earlier note calls 10000 "barely fitting" and "touching both edges", but 28262 is UNREADABLE — the final digit is cut through its middle, so the child cannot verify what they typed before pressing Check. RULING NEEDED AT FIX TIME: the row proposes sizing the input to the expected digit count "known from the key". That is wrong — the box holds what the CHILD types, not what the key says, so a child who typos 282620 into a box sized for five digits is clipped again. Auto-fit, or key-digits-plus-headroom, is the only form that survives over-typing.

| 86 | 4 | `q8j27p4t5` | — | Multi-select ("select every correct one") wrong-state marks only the INCORRECTLY-picked option (red X); a correctly-picked option gets no mark and its checkbox clears, so the child cannot distinguish their right pick from their wrong one, and may read the cleared correct pick as also wrong. Confirmed q8j27p4t5 Q25. Fix: green check on each correctly-selected option, red X on each incorrectly-selected one. OPEN at fix time: how to treat correct options the child did NOT select (revealing them hands over the answer). PATTERN-WIDE: every multi-select. Ties to the wrong-state redesign (Ruling 15) — its "select all" case. | E | layout | open | 2026-07-22 | — | — |
| 87 | 4 | `q8j27p4t5` | — | Multi-select wrong answer shows no clear instruction — only a hint appears. Should show an explicit message such as "Select all the correct options." Confirmed q8j27p4t5 Q25. PATTERN-WIDE: every multi-select. | E | polish | open | 2026-07-22 | — | — |
| 88 | 4 | `qrgncsxq7` | — | On retrying a wrong fill-blank, the previous (wrong) answer is not cleared — the child must manually delete it before re-entering (extra step). Clear the blank when the child begins a retry, for a clean slate. Confirmed qrgncsxq7 Q14. PATTERN-WIDE: every fill-blank. NOTE: only relevant while Grade 4 keeps the try-again model; moot if Grade 4 adopts auto-reveal / no-second-attempt (open pedagogy decision). | E | polish | closed | 2026-07-22 | 2026-07-23 | CLOSED by BRIEF-RETRY-STATE-2 (G1) 2026-07-23. Venkat RULED the typed value IS CLEARED on Try again — REVERSES the FR-2 ruling-4 law that it is NEVER cleared. rao-card.js restoreTask() no longer re-preserves fill-blank values via keep[]; the mount snapshot already carries empty inputs, so the retry starts clean. The value SURVIVES the wrong Check (the child still sees the answer they are told is wrong) and clears only on the Try-again tap. LAWS law-3 amended and dated. verify-reset.js A5-through-Try-Again and the keepValues drill INVERTED preservation to clearing. See the reversal record row below. Guard verify-retry-state.js G1. |
| 89 | 4 | `q8juai6df` | `rao-card.js` | The auto-reveal fallback (Item #50 showAnswer path) opens with "Here's the answer — you've got this!" shown AFTER the child failed (wrong x2). "You've got this" reads as praise for a correct answer and misleads the child about whether they succeeded. Confirmed q8juai6df Q29. ENGINE-WIDE: every question hitting the fallback. Fix: reword to an honest, encouraging-but-not-congratulatory line that does not imply success (e.g. "Here's the answer — let's look at it together"); reserve congratulatory tone for correct answers only. | E | polish | open | 2026-07-22 | — | — |
| 90 | 4 | `q8juai6df` | — | Revealed categorize answer prints raw internal tokens — "Answer lt80000, ge80000, ge80000, lt80000" — instead of child-readable bin labels ("Below 80,000" / "80,000 or more"), with no mapping of which sum goes in which bin. Unintelligible to a fourth grader. Confirmed q8juai6df Q29. Root: no authored solution (see #49) → fallback dumps the raw answer array. PATTERN-WIDE: categorize questions hitting the fallback (verify other types at fix time). Fix: translate bin keys to the on-screen labels and map each item to its bin; part of the broader solutions cleanup (#49). | E | layout | open | 2026-07-22 | — | — |
| 91 | 4 | `qw3vv7rah`, `qhxw9drpb` | `lessons/interpret-remainders.html` | SUSPECTED WRONG ANSWER KEYS. Q4 (`qw3vv7rah`): "99 people need to ride the lift to the top of a skyscraper. The lift can hold 6 people at a time. How many people will be in the lift on the last trip to the top?" 99 ÷ 6 = 16 r 3 → answer 3; page shows **Answer: 7**. Q5 (`qhxw9drpb`): "40 sofas… 7 sofas in each truck, how many sofas will be in the partially full truck?" 40 ÷ 7 = 5 r 5 → answer 5; page shows **Answer: 3**. TWO OF TWO visible questions appear wrong. READ OFF A SCREENSHOT BY CHAT — confirm against the file before acting; chat retracted a similar claim about `qyaewyvs9`/`qn7anm656` the same session after re-checking. If it holds, the whole 29-question lesson needs an independent recomputation. THIS CLASS OF DEFECT IS INVISIBLE TO EVERY EXISTING GUARD: a wrong key grades correctly against itself, so the grading gate goes green. GATES #92. | L | correctness | not-a-defect | 2026-07-22 | — | CLOSED not-a-defect 2026-07-22. This row was CHAT-GENERATED from a misread screenshot and was never reported by Venkat — it carries no reporter quote, unlike every other row in this batch. Refuted against the file by BRIEF-RECOMPUTE-91: all 29 questions recomputed independently, every stored key equals the remainder of its division, NONE count 0, UNPARSED count 0. The two ids named (`qw3vv7rah` 99÷6 = 16 r 3, key 3; `qhxw9drpb` 40÷7 = 5 r 5, key 5) are both correct. Second same-method retraction this session (the first being `qyaewyvs9` / `qn7anm656`). RULE ADOPTED: a correctness claim that does not originate from Venkat does not get an issue number until it is confirmed against the file — a suspicion may be raised in conversation, but the log records confirmed defects. This closure is also the evidence base for #92: the MCQ conversion was built on 29 independently verified keys. |
| 92 | 4 | — | `lessons/interpret-remainders.html` | Convert all 29 questions from fill-blanks to MCQ. Venkat verbatim: "I want to convert all the questions of this lesson from fill in the blanks to m c q format." and "in fourth grade i don't want kids to do a lot of fill in the blanks so let's just make everything mcq in this lesson". SIZE: 29 × 3 = 87 distractors. Do NOT invent them — `docs/MISCONCEPTIONS.md` carries REMAINDER_AS_ANSWER, QUOTIENT_OFF_BY_ONE and DIVISOR_AS_ANSWER for exactly this content, so distractors generate systematically and their whyWrong messages come free. Natural first real test of the #49 classifier work. GATED ON #91 — converting around a wrong key wraps the error in three distractors. Tightens #53 (MCQ was a Grade 4 *preference*); see AMENDMENTS. | L | polish | open | 2026-07-22 | — | STATUS: work complete, awaiting commit — not yet in origin. PHASE 1–3 executed 2026-07-22 (chat) — see REPORT-MCQ-CONVERT-92-PHASE1/2/3.md. All 29 fill-blanks → single-select at `lessons/incoming/interpret-remainders.html` (note: file is under `incoming/`, not the `lessons/` path this row records); ids/prompts/explains preserved, only format changed; lesson-local whyWrong per Venkat (taxonomy messages read backwards for a remainder-answer question, so authored fresh with `code:` kept — `docs/MISCONCEPTIONS.md` untouched); one 3-option question (`qix6jkchx`, 19÷5 admits only 2 honest distractors — ruled option b, no number change). Guarded by `tools/verify-mcq-92.js` (sabotage-proved); `test:fast` + explicit `verify-format.js` green; review regenerated. COMMITTED `ff2c0d4` 2026-07-23 (10 files, +2457/-207) after Venkat reviewed `review/interpret-remainders.html` and approved. Pre-commit hook ran `test:fast` green, and `verify-format-staged` fired for real this time (lesson+review pair was staged): PASS interpret-remainders (29 cards). Floor locked in the same commit (`docs/whywrong-floor.json`, interpret-remainders 0→29, ratchet proved by stripping one whyWrong → guard FAILED at 28 → restored byte-identically → PASS). SURFACED #111's second confirmed instance (stale whyWrong panel steering the retry in MCQ) — that engine bug (Batch 2) must be fixed BEFORE converting further lessons; see #111 amendment. |
| 93 | 4 | — | `lessons/Estimate_sums__word_problems.html` | Convert the 5 remaining fill-blanks to MCQ (lesson is 30 questions: 5 fill-blanks, 25 single-select). Venkat verbatim: "I want all the questions in this lesson to be of mcq type." Example: `qvfqrpt5k` (ducks, 43 + 48 round-then-add). 15 distractors. ESTIMATE_WRONG_VALUE and the rounding codes cover them — for the ducks question the natural set is 91 (didn't round), 80 (rounded both down), 100 (rounded both up), each a real misconception. Same Grade 4 fill-blank-load ruling as #92. | L | polish | open | 2026-07-22 | — | — |
| 94 | all | `q2ebqrscc` | — | Figure dimension labels are OVERSIZED — corpus-wide, not lesson-specific. Venkat verbatim: "look at the font size of the dimensions of the figure. They look a little oversized. And this issue is not isolated. it seems to affect a lot of questions with these kind of geometric figures." Evidence: `q2ebqrscc` (square portrait, gold frame ₹1,946/m). The "1 m" side labels render at roughly the same visual size as the question body text; a dimension label should sit clearly BELOW body text in the hierarchy. Affects every geometry/area/perimeter question carrying a figure — one CSS value in the figure renderer, not per-lesson edits ("fix the general case"). **MANDATORY PRECONDITION — MEASURE FIRST.** Venkat's notes record TWO occasions where tile/chip font sizes were "restored" from stale notes, silently undoing deliberate design decisions. Measure the CURRENT value against the repo engine; choose the new value against that measurement, never against a remembered or documented number. | E | layout | open | 2026-07-22 | — | — |
| 95 | 4 | `qyaewyvs9`, `qn7anm656` | — | Remove the operation-name prefix from bare-expression questions. Venkat verbatim: "Remove, um, words like divide or multiply for these kinds of questions. The kid can automatically see and figure out on their own if it's a division or multiplication question. Adding the operation name is not necessary." So "Divide: 840 ÷ 4 = ▢" becomes "840 ÷ 4 = ▢". Same rule as #101 ("Fill in the blank:"). GENERAL RULE, ruled by Venkat across six instances this session: **remove prompt text the interface already communicates; keep text that adds information the child cannot otherwise get.** The operator symbol already names the operation. THE EXCEPTION Venkat drew himself: "Round each number to the nearest thousand" STAYS, because nothing on screen tells the child to round. Note: answer keys for these two questions were checked and are CORRECT (840÷4=210, 2,862÷3=954) — chat initially misread them and retracted. | P | polish | open | 2026-07-22 | — | — |
| 96 | 4 | `qfh2vmnv7` | — | Coordinate-grid construct figures are too tall — the child must scroll to see the full axis. Venkat verbatim: "These geometric figures are very tall, kids will have to scroll down to see the full coordinate axis. Can we make them less tall". Evidence: `qfh2vmnv7` ("Build a rectangle with an area of 9 square units") renders a grid to 10 on both axes when the answer occupies a 3×3 area. TWO SEPARABLE FIXES: (a) cap rendered height in the figure renderer — cheap, corpus-wide; (b) size the grid range to the question — per-question content work, slower. Do (a) first. **CAUTION — these are construct questions (the 31 that were dead until #75).** The grid is where the child TAPS. Shrinking height shrinks tap targets. Hard floor: cells never below the ~44px tap minimum. If a grid cannot fit both, it must shrink its RANGE, not its cell size. Related to #94 — same renderer, likely the same brief. | E | layout | open | 2026-07-22 | — | — |
| 97 | all | `q2ebqrscc` | — | Long TEXT answer options are oversized and hard to scan — centred prose in full-width boxes. Venkat verbatim: "whenever we have questions that have long answer choices, they look kind of big on the screen. Can you suggest a slightly different layout? Maybe by reducing the font weight or size and or aligning them to the left hand side so that we can make the questions more compact." **RULED (Venkat, after seeing mockups): left-aligned, 15px, regular weight, tighter padding.** Left alignment does most of the work — every option starts at the same x so the eye scans one edge. Reclaims roughly a third of the vertical space. THREE CONDITIONS ATTACHED: (1) NUMERIC options stay CENTRED — a column of 800/828/900/700 reads better centred, and left-aligning them would fix the prose case and worsen the numeric one; (2) two-per-row ONLY when every option fits on one line, else one per row — uneven box heights read as though the tall one matters more; (3) TAP TARGETS STAY ≥44px — the 15px/tight-padding combination sits close to that line, so it must be MEASURED in a browser, not eyeballed, and verified after a real click at multiple viewport sizes (visibility law). Consequence: the engine must distinguish option content types. `qjibia3y5` (#102) shows a THIRD case — table-bearing options — so this may need three branches, not two. | E | layout | open | 2026-07-22 | — | — |
| 98 | 4 | `qe5j5q7hb` | — | Sequence-build tile text OVERFLOWS and the layout collapses. Venkat verbatim: "The text is overflowing." Evidence: `qe5j5q7hb` ("Priya wants to estimate 57 + 34… Build her plan in order"). Tiles are sized for short labels but carry full phrases ("Add the rounded numbers", "Pick the place to round to"). Text breaks out of every tile, tiles overlap each other AND the drop slots above; "Pick the place to round to" runs four lines deep and collides with neighbours. A child cannot read the options, let alone drag them. SAME DEFECT CLASS AS #81 — a container with no rule for content wider than itself — but a QUESTION-side container, which `SOLUTION-PANEL-LAYOUT-CONTRACT-v1.md` does not reach. Open question for the fixer: widen the contract's scope to question-side containers, or write a separate rule. NOTE: `.tile` has a `--ow` min-width computed from the longest tile text; that mechanism exists and is not handling multi-word labels. MEASURE the current values against the repo before changing anything (see #94's precondition). | E | layout | open | 2026-07-22 | — | — |
| 99 | 4 | `qm37aecdj` | `lessons/estimate-sums-faithful.html` | Redundant first sentence on round-scaffold questions. Venkat verbatim: "look at all the questions of this variety. The initial first line is redundant. We can take them out." So "About how much is 3,007 + 6,582? Round each number to the nearest thousand, then add." becomes "Round each number to the nearest thousand, then add." The scaffold below ALREADY renders 3,007 + 6,582 with arrows down to the blanks. Worse, the removed sentence asks a question the scaffold answers differently — "about how much is" invites ONE number; the answer is THREE blanks. SCOPE: "all the questions of this variety" = the round-scaffold ones (lesson has 13 fill-blanks total); enumerate that subset before editing. Sibling of the existing rule "lead with the actual question/expression, then the rounding or method instruction" — the general principle is that when the expression is already rendered in a scaffold, the prompt must not restate it. Round-scaffold questions exist across multiple estimation lessons. | P | polish | open | 2026-07-22 | — | — |
| 100 | 4 | `q2tz3k3hi` | — | Method instruction buried at the END of a multi-select prompt, where the child may miss it. Venkat verbatim: "in the problem statement, I believe the order of sentences should be reversed or the whole problem statement should be rephrased. Because by reading the first sentence, the kid might just directly start adding the numbers without, uh, realizing that the question is that of rounding." Current: "Select ALL the problems with a sum of about 6,000. Round each number to the nearest thousand." Reversed: "Round each number to the nearest thousand. Select ALL the problems with a sum of about 6,000." **THIS TENSIONS WITH THE EXISTING RULE** ("lead with the question, then the method instruction"). The resolution, and the general principle worth writing down: **the child must know what KIND of thinking is required before they see the numbers.** For "840 ÷ 4" the operator already says so. For rounding, nothing does — so it must come first. Method goes last only when it is a formatting note; first when it changes what the task IS. Applies to every estimation lesson. ALSO CHECK AT FIX TIME: chat computed two correct options (2,116+3,895 → 6,000 ✓; 4,872+1,309 → 6,000 ✓) and three wrong; confirm the key carries BOTH correct answers. | P | polish | open | 2026-07-22 | — | — |
| 101 | 4 | `qft2bv57f` | — | Remove "Fill in the blank:" prefix. Venkat verbatim: "No need to write fill in the blanks, the kids will understand what to do." So "Fill in the blank: 4 km 200 m = ▢ m" becomes "4 km 200 m = ▢ m". The input box already communicates the task. SAME RULE AS #95 — one editorial rule, two instances. Covers "Fill in the blank:", "Divide:", "Multiply:", "Solve:" and equivalents in front of a bare expression. | P | polish | open | 2026-07-22 | — | — |
| 102 | 4 | `qjibia3y5` | — | Table-bearing answer options are OVERSIZED. Venkat verbatim: "In this question, the answer choices are oversized. If it is too hard to compress, let's remove these types of questions." Evidence: `qjibia3y5` ("A stall sold 42 kilos of bananas and 28 kilos of apples. Which table shows this correctly?") — each option renders at full standalone-table scale: nested card wrapper, generous cell padding, solid purple header bar repeated per option. Four stacked would run several screens. CHAT'S RECOMMENDATION — compress rather than delete the type: drop the outer card wrapper, tighten cell padding, lighten the header treatment, shrink text one step. Two tables differing only in which number sits on which row do not need full presentation scale; the child is comparing two rows, not reading a report. Comparing tabular representations is a real Grade 4 skill — the problem is presentation, not pedagogy. Only cut the type if compression genuinely fails. Interacts with #97: a table option is neither text nor numeric — a THIRD option-content branch. | E | layout | open | 2026-07-22 | — | — |
| 103 | 4 | `qscbcghnr` | — | Prompt gives away the exact thing being assessed. Venkat verbatim: "This should be something the kid should find out after making a mistake. if we tell them in advance, it kind of takes the learning away." Current: "Tap every number that is less than 20,000. Be careful: 20,000 itself is not less than 20,000." Remove the second sentence. **STRONGER THAN THE OTHER PROMPT REMOVALS (#95/#99/#101/#104), which remove NOISE — this removes the ASSESSMENT.** 20,000 is in the options specifically to test strict inequality; the warning hands over the answer, so the distractor can no longer catch anyone and the question tests reading the warning rather than understanding "less than". WHERE IT SHOULD GO: the whyWrong for the 20,000 distractor — a child who taps it learns, at the moment it matters, that a number is not less than itself. Concrete argument for #49: scaffolding removed from prompts needs somewhere to go, and whyWrong is that somewhere. | L | correctness | open | 2026-07-22 | — | — |
| 104 | 4 | `qc9nue7ia` | — | Categorize prompt restates the bin labels verbatim. Venkat verbatim: "The problem statement can be simplified. There's no need to add the portion which is circled in red. It's not adding any value." Current: `Drag each number into "Over 25,000" or "Under 25,000".` The two bin headers sit directly below saying exactly that. **VENKAT EXPLICITLY RULED THIS CORPUS-WIDE:** "this issue is there in many other similar problems. So it's not a one off issue." SIX instances found across six lessons in a PARTIAL pass of ~12 of 103 lessons (#95, #99, #101, #103, #104, and the quotient-definition case #105) — the true corpus count is substantially higher. SWEEP APPROACH: a script can flag candidates mechanically — prompts opening with an instruction-verb prefix, prompts containing text that also appears in the options or bin labels, prompts restating an expression rendered below. The script FLAGS; Venkat RULES, because the exception (does the removed text carry information the interface cannot?) needs judgement. Close relative of #78 (procedure-first phrasing sweep) — probably one brief. ALSO NOTED: the tiles include 25,001 but not 25,000, suggesting someone dodged the boundary case; confirm the categorize key handles "equal to" correctly, since a bin pair with no equal-to option is one authoring slip from unanswerable. | P | polish | open | 2026-07-22 | — | — |
| 105 | 4 | `qdyuf9p63` | — | Definition line handed to the child in the prompt, on all 23 questions in the lesson. Venkat verbatim: "Remove this state from all the problems in this lesson." The line: "Quotient = bigger number ÷ smaller number", rendered as a helper chip above "The sum of two numbers is 4. Their quotient is 1. Tap the pair." It is not a known fact being applied — it is a definition, and for this question it also does the child's disambiguation: without it, "their quotient is 1" could mean either order, and working out that it must be bigger ÷ smaller IS part of the thinking. **VENKAT RULED where it belongs: "No, that will be shown in hint."** GENERAL PRINCIPLE worth recording: **prompts carry the question, hints carry the method.** A definition in a prompt is a hint that fires unconditionally for every child. ALSO: the helper line renders left-aligned at the card edge while the question below is centred — they are not aligned to each other, which is why it reads as a stray sentence rather than a scaffold. | L | polish | open | 2026-07-22 | — | — |
| 106 | 4 | `qyxiqaxrk` | — | Line-plot prompt does not reference the figure it depends on. Venkat verbatim: "The problem statement is a little ambiguous, feels a little inadequate. I wonder if there are other questions like this in this lesson." Current: "Tap how many team members there are in all." It never says there IS a line plot, never says what an × represents, and never connects "team members" to anything labelled on the chart (the axis says "Goals scored"). The child must infer that each × is one team member — which IS the reading skill being tested, but it is being tested by omission rather than asked. Suggested: "The line plot shows how many goals each team member scored. Tap how many team members there are in all." SWEEP CANDIDATE, and mechanically detectable: flag every question whose prompt makes NO reference to the figure/table it depends on, or which uses a deictic ("this statement", "the figure below", "the table shows") with no corresponding rendered element — that second form also catches #107. Script flags, Venkat rules. ALSO: chat counted 8+7+9+3 = 27 marks and option 27 exists, but that is a screenshot count — confirm against the file. | P | polish | open | 2026-07-22 | — | — |
| 107 | 4 | `qhhmpihb4` | — | QUESTION IS UNANSWERABLE — the statement it refers to does not render. Venkat verbatim: "I think this question is missing something." Prompt: "A digit is missing. Tap every digit that makes this statement true." There is NO statement on the card — no expression, no inequality, nothing with a gap. Options are 3/4/5/6; key is 5, 6. Whatever comparison was meant to render (something like `4▢,127 > 45,000`) is absent. **EVERY EXISTING GUARD PASSES THIS QUESTION**: options render, the grading gate accepts 5 and 6 and rejects 4 and 6's complement. It is internally consistent and completely unanswerable. Third defect of this class found tonight by eye (see #91, #110) — invisible to tooling, obvious to a human. PARTIALLY DETECTABLE: see the deictic-reference sweep in #106. | L | correctness | open | 2026-07-22 | — | — |
| 108 | 4 | `q4n7uzydi` | — | One question demands FOUR full five-digit additions. Venkat verbatim: "this question is basically four questions bundled into one. It's not fair to ask a fourth grader to solve all four of them to figure out where the issue is. It's too much to ask. So I wanna remove these types of questions. Unless, of course, you have a way of simplifying things." Current: "Some of these totals have a carrying mistake. Select every addition that is correct" with four five-digit sums (37,409+51,286=88,695 ✓; 28,615+44,190=72,705 ✗ — true sum 72,805; 46,203+35,708=81,911 ✓; 19,847+60,152=79,999 ✓). Chat checked for an honest shortcut (last-digit, estimation) — none discriminates this set, so the child genuinely must compute all four. **RULED (Venkat): split into single-sum questions, each framed as checking a NAMED CHILD's work.** Venkat verbatim: "instead of simply asking to check if the addition statement is correct or not, frame it like some kid did this calculation. Are they right or not?" Fits `WRONG_ERROR_DIAGNOSIS` in the taxonomy (worked steps with one known error) and the existing Priya/Arjun/Rohan/Diya convention. CHAT'S DESIGN NOTE: use THREE options, not two — "Yes that's right / No, it should be 72,805 / No, it should be 73,705" — because right-vs-wrong gives 50% on a guess, while three forces the child to know WHICH carrying error occurred, and the distractors map to real misconceptions so whyWrong comes from the taxonomy. CONFIRM the key at fix time (chat computed 28,615+44,190 as the wrong one from a screenshot). | L | polish | open | 2026-07-22 | — | — |
| 109 | all | `q2zyrs8kF` | — | GRADER REJECTS A COMMUTATIVELY-CORRECT TYPED ANSWER. Venkat verbatim: "A naughty kid? might try commutative property and discover a bug in our system that we don't treat commutatively stated operations the same way." Evidence: `q2zyrs8kF` "Write the addition sentence for thirty-one plus sixteen, and give the total." A child typed `16+31=47` — mathematically correct — and was marked WRONG. **SAME CLASS AS #84: a correct child marked wrong.** Arguably worse, because the child who tries it is the one who UNDERSTANDS commutativity, so the system punishes the strongest instinct in the room. THE DESIGN QUESTION UNDERNEATH: if operand ORDER is part of what is assessed (translating word order to digit order), `16+31` is not the requested sentence even though it is a true one. CHAT'S RECOMMENDATION — accept it: the child demonstrated everything the question tests (reading number words, converting to digits, adding correctly), and marking it wrong teaches "there is a hidden rule about order", which is false. SCOPE: engine-level grading, every question accepting a typed expression; check whether `4×6` vs `6×4` has the same problem and whether the grader normalises at all. **CAUTION FOR THE FIX: commutativity holds for + and × ONLY.** A grader that normalises operand order must do it PER-OPERATOR or it will start accepting `4-9=5`. | E | correctness | closed | 2026-07-22 | 2026-07-23 | CLOSED by BRIEF-RETRY-STATE-2 (G5) 2026-07-23. Engine check() expression branch order-normalises a pure a+b+...=total sentence by sorting the addends — ADDITION ONLY via an explicit {+} operator allowlist. Venkat re-ruled +-only (superseding the earlier +/x ruling) because Phase 0 measured the corpus at 6 typed-expression questions, ALL addition, zero x, minus or div. Subtraction and division written backwards STAY wrong (sabotage-proved with SYNTHETIC 4-9=5 and 3div12=4, NOT added to lessons). x is deliberately excluded and commented at the allowlist — revisit only if a typed-x question is ever authored. Guard verify-retry-state.js G5. |
| 110 | 4 | `qjiieem9t` | — | Multi-conversion fill-blanks render as one long sentence with scattered blanks — the pattern is invisible. Venkat verbatim: "These types of questions should have, uh, sort of a tabular format so that the kid can see the pattern. Otherwise, it just looks like a very long drawn out sentence with a lot of blanks, and there is no pattern to be found." Evidence: `qjiieem9t` — "1 centilitre = 10 millilitres. Fill in: 1 cL = ▢ mL, ▢ cL = 20 mL, ▢ cL = 40 mL, 6 cL = ▢ mL, ▢ cL = 70 mL". **RULED (Venkat, after mockups): render as a TABLE with the units as COLUMN HEADERS.** Venkat verbatim: "just having them be part of a table. where c l and m l can be the column names. We have several of those types in our existing question bank." Units move to the headers so cells hold only numbers; reading down the mL column (10, 20, 40, 60, 70) makes the relationship almost impossible to miss. It also exposes that the blanks ALTERNATE sides — sometimes the cL is given, sometimes the mL — which the prose form hides completely. IMPLEMENTATION: a NAMED LAYOUT the renderer applies (structurally like the existing `round-scaffold`), not a per-question hand-build, so every multi-conversion question inherits it. CHECK FIRST whether the existing "Complete the table" renderer (e.g. `qwkacbdmg`) can simply accept a header row of unit names — one component beats two, and the child learns one interaction. FINDING THE OTHERS is mechanical: fill-blanks questions with ≥3 blanks in a repeating "X unit = Y unit" shape. | E | layout | open | 2026-07-22 | — | — |
| 111 | all | `auth-q3` | `rao-card.js` | STALE WRONG-ANSWER FEEDBACK SURVIVES A CORRECT RETRY. Venkat verbatim: "I got a question wrong, then I clicked on try again. And this time, I got the answer right. But the message there still persists from the first time I selected the wrong answer. This can be very confusing to a kid." Evidence: `auth-q3` (6 × 9) — option 54 shows GREEN as correct while the red "NOT QUITE" panel from the previous attempt still sits below explaining why 45 was wrong. The child sees success and failure simultaneously. **LIKELY A CONSEQUENCE OF THE NO-REPAINT / APPEND-ONLY LAW**: the whyWrong panel appends and nothing clears it on a subsequent correct answer. So the fix is a real design decision, not a one-liner — either the law needs a stated exception for stale wrong-answer feedback on a correct outcome, or the panel must visibly RESOLVE (struck through, greyed, captioned) rather than disappear. Chat leans toward clearing it: a child who just got it right does not need the archaeology of their first attempt. **GUARD GAP:** `verify-solpanel.js` assertion 3 covers bubbles clearing when the SOLUTION opens (#63), and #66 established whyWrong as a separate stream — but NOTHING covers wrong→correct. Same underlying problem as #88 (stale fill-blank surviving a retry): fix in one pass with a guard covering all state transitions. | E | correctness | closed | 2026-07-22 | 2026-07-23 | CLOSED by BRIEF-RETRY-STATE-2 (G2) 2026-07-23. rao-card.js hideStaleFeedback() HIDES (display:none, node RETAINED — no-repaint / append-only law) the .cc-msg-why whyWrong panel and its Not-quite chip, and clears any prior ✕/tint, fired by a capture-phase qbody listener on any new option tap or blank edit (delegated on qbody so it survives restoreTask re-mount). Hint bubbles are untouched (law 4). NOT dismissed on the Try-again button itself — that would break verify-reset law-4 (bubbles survive the reset) and is not the ruling's trigger (a NEW SELECTION is). FLICKER FINDING (Phase 3.1): hiding is ONE-DIRECTIONAL — a new selection only ever hides, and the panel re-appears only on a later wrong Check — so rapid tapping does NOT flicker it in and out. Demo review/_RETRY-STATE-2-flicker-demo.html + .gif — VENKAT SHOULD SEE IT before ship. Guard verify-retry-state.js G2 (panel hidden, chip hidden, ✕/tint cleared, node still present). PRIOR NOTES: AMENDED 2026-07-22 — SECOND CONFIRMED INSTANCE + a ruling that WIDENS the fix. Evidence `qp4u9d7tx` (interpret-remainders Q2/29, AFTER the #92 MCQ conversion): child selects a wrong option → Check → NOT QUITE whyWrong panel ("7 is how many miners a full lift holds — that's a full trip, not the last one") → Try again → the panel is STILL on screen; the child takes the next attempt while reading the critique of the previous one. WORSE IN MCQ THAN FILL-BLANKS: the whyWrong names a SPECIFIC option, so stale feedback actively STEERS the retry rather than merely cluttering it — the child re-reads their own last mistake instead of re-reading the question. NOT caused by #92: pre-existing engine behaviour. But #92 makes it far more visible — fill-blanks rarely surfaced a per-option message, so converting 29 questions turned a latent bug into one a child meets repeatedly in one lesson; if #92's pattern spreads to other lessons the frequency compounds. FIX BEFORE FURTHER CONVERSIONS. RULING (Venkat 2026-07-22): the fix is WIDER than "Try again" — ANY NEW SELECTION dismisses the previous feedback, not just the Try-again button. The moment the child taps a different option, the whyWrong panel, the NOT QUITE chip, and any prior-attempt correctness styling must go. Rationale: a child who has moved on from an answer should not still be reading the critique of it. IMPLEMENTATION CONSTRAINT (binding): the no-repaint law forbids rebuilding the question DOM mid-session and solution/hint panels are append-only, so "clear the panel" MUST be implemented as HIDE (visibility/display), NEVER node removal — a fix that removes the panel node violates an existing law. OPEN AT FIX TIME: hiding on every selection-change makes the panel flicker in/out on rapid tapping — probably acceptable (arguably the correct signal) but MUST be seen in review before shipping, not discovered after. BATCH: strengthens Batch 2 (retry-state engine pass — #111, #109, #84, #85, #88; one root cause: state that does not reset). With #109 confirmed harm (correct child marked wrong on 16+31=47), Batch 2 is now the strongest candidate for the next engine brief, AHEAD of Batch 1. |

AMENDED 2026-07-22. SECOND CONFIRMED INSTANCE, and a ruling that widens the fix. Evidence `qp4u9d7tx` (Q2/29, interpret-remainders, post-MCQ-conversion): child selects a wrong option, presses Check, gets the NOT QUITE whyWrong panel ("7 is how many miners a full lift holds — that's a full trip, not the last one."), presses Try again — panel still on screen. The child takes their next attempt while reading the critique of their previous one. WORSE IN MCQ THAN FILL-BLANKS: the whyWrong names a SPECIFIC option, so stale feedback actively steers the retry rather than merely cluttering it. Venkat verbatim: "After i hit try again, these kinds of messages should disappear and give kids a fresh take on the question." NOT CAUSED BY #92 — pre-existing engine behaviour — but MCQ conversion makes it far more visible: fill-blanks rarely surfaced a per-option message, so converting 29 questions turned a latent bug into one a child meets repeatedly in a single lesson. Frequency compounds if the #92 pattern is applied elsewhere; **fix before further conversions.** RULING (Venkat, 2026-07-22) — the fix is WIDER than "Try again": **ANY NEW SELECTION dismisses the previous feedback.** The moment the child taps a different option, the whyWrong panel, the NOT QUITE chip, and any prior-attempt correctness styling must go. Rationale: a child who has moved on from an answer should not still be reading the critique of it. IMPLEMENTATION CONSTRAINT — READ BEFORE FIXING: the no-repaint law forbids rebuilding the question DOM mid-session and solution/hint panels are append-only, so "clear the panel" must be implemented as HIDE (visibility/display), NOT node removal. A fix that removes the panel node violates an existing law. OPEN AT FIX TIME: hiding on every selection-change means rapid tapping flickers the panel in and out — probably acceptable, arguably correct signal, but must be seen in review before shipping. BATCH: strengthens Batch 2 (retry-state engine pass — #111, #109, #84, #85, #88; one root cause: state that does not reset). With #109 confirmed harm, Batch 2 is now the strongest candidate for the next engine brief, ahead of Batch 1.

| 112 | 4 | `qn5ksv3ht` | `lessons/incoming/rounding_remix.html` | Pink marker label duplicates a tick label already printed on the number line. Venkat verbatim: "if the number that we are asking to round is already there on the number line, we don't need to write it again in the pink color. For example, fifty five was already there on the number line. There was no need to write fifty five again below the number line and below the existing numbers." Evidence `qn5ksv3ht` (Q4/30, "55 is exactly halfway between 50 and 60. Which way do we round?"): axis labelled 50–60 in UNIT steps, so the tick under the marker already reads 55; the renderer prints 55 again in pink ~6px below it — same number twice, two colours, two type sizes. **FIX CONDITION IS NARROW — the pink label is NOT always the marker position.** Venkat confirmed it can carry other content. Remove ONLY when its text is identical to the tick label at the same x-position. **NEGATIVE CASE, MUST NOT REGRESS: `qpq6x6fej`** (Q7/30, "Which 100 is 271 closest to?") — axis labelled in TENS (200,210,…,300), marker at 271, which is NOT a tick label. There the pink 271 is the only place the child can read the target; the question is unanswerable without it, and the dot sits nearest the 270 tick so removal would actively mislead. Any sweep that removes pink labels generally breaks this question. PRECONDITION: confirm the ▼ pointer and pink dot render unconditionally — they become the sole target indicator once the label goes. | E | layout | open | 2026-07-22 | — | — |

| 113 | 4 | `q8gwp2qc7` | `lessons/incoming/rounding_remix.html` | Round-then-order question where the rounding is already done and the tiles are pre-sorted — the question tests neither skill. Venkat verbatim: "this question doesn't offer any value to the kid because the rounding has already been done, and all we're asking them to do is arrange them in an ascending order." Evidence `q8gwp2qc7` (Q30/30): prompt reads "Round each number, then drag them smallest → largest: 76 (to 10), 149 (to 10), 705 (to 100)" but the tiles carry 80, 150, 700 — already rounded, already ascending. Correct answer is to move nothing. The prompt claims a rounding assessment; the child passes by touching nothing. **VALIDITY RULE (ruled by Venkat): a round-then-order question is honest only if sorting the RAW values yields a DIFFERENT order than sorting the ROUNDED values.** Mechanically checkable — sort both, compare — so enforceable by guard rather than judgement. SECOND REQUIREMENT: tiles must never be presented in the answer order. SCOPE: Venkat ruled LESSON FIX, not corpus sweep, and ruled that no new questions be generated. Lesson header confirms this is the ONLY `order` question in the lesson (30 questions: fill-blanks 4, multi-select 2, order 1, single-select 23), so this is a one-question fix. | L | correctness | open | 2026-07-22 | — | — |


| 114 | all | — | `docs/MISCONCEPTIONS.md` | **Division misconception codes assume the QUOTIENT is the correct answer; on remainder-answer questions every message is factually inverted.** Found by BRIEF-MCQ-CONVERT-92 Phase 1 — Claude Code stopped and reported rather than shipping. Worked example: in `interpret-remainders`, a child who taps the quotient — the most common real mistake — is shown `REMAINDER_AS_ANSWER` verbatim: "This is the little bit left over, not the answer — the question wants the big share." The child picked the big share; the message says they picked the leftover. Backwards for the single most common wrong answer in the lesson. ROOT CAUSE IS STRUCTURAL, NOT MISSING CODES: the taxonomy has no representation of what the question ASKS. The same number (the quotient) is a wrong answer in a remainder question and the correct answer in a quotient question. Adding mirror-image codes patches this shape and leaves the next one ("how many trips altogether" = quotient+1) broken. **DO NOT FIX YET.** `docs/MISCONCEPTIONS.md` is unaudited — HANDOFF-44 §6 records suspected miscounts (PARTIAL_PRODUCT_PLACE_ERROR rows 13 and 29, WRONG_UNIT_CHOSEN rows 21 and 59; the 4,014 total may be wrong) with instruction to log, not fix. BRIEF-WHYWRONG-CLASSIFY-1 Phase 1 must run against the file UNCHANGED. **ADD TO THAT BRIEF'S PHASE 1:** check all 63 codes for the same assumption — if division has it, other operation families may too. WORKAROUND IN USE: #92 writes lesson-local whyWrong text and keeps the `code:` field for analytics; taxonomy untouched. | E | correctness | open | 2026-07-22 | — | — |

| 115 | 4 | — | `lessons/incoming/interpret-remainders.html` | **Known limitation introduced by the #92 MCQ conversion: in 23 of the 28 four-option questions, all three distractors are LARGER than the correct answer.** Unavoidable arithmetic in remainder problems — the remainder is always less than the divisor, which is at most the quotient — so the correct answer is nearly always the smallest number on screen. Option order is shuffled (salt `r2:`, correct-answer position spread {0:7, 1:7, 2:7, 3:8}, max 8, within the ≤10 rule), which hides WHERE the answer sits but not THAT it is smallest. A sharp child can learn "pick the smallest number" and stop dividing. The only lever available within the conversion was the remainder±1 distractor, which fires only on collisions. **The real fix is outside #92's scope:** vary what the questions ASK across the lesson — some quotient ("how many full trips"), some quotient+1 ("how many trips altogether"), some remainder — so the correct answer is not systematically the smallest. Venkat ruled question rewrites out of scope for the conversion, so this is logged rather than fixed. NOT A REGRESSION: fill-blanks had no options to compare, so the tell did not exist before conversion. **Applies to any future remainder lesson converted to MCQ — read this row before repeating the #92 pattern.** | L | design | open | 2026-07-22 | — | — |
| 116 | all | — | `tools/verify-line-endings.js` | **The line-ending guard scans `lessons/` only; every other directory containing `.html` is unguarded.** `.gitattributes` declares `* text=auto eol=lf` — repo-wide LF — but `verify-line-endings.js` walks `lessons/` recursively (and even there skips `_preview` by name), so nothing else is checked. UNGUARDED: `review/` (105 generated pages — where mixed CRLF was actually found), `lessons/_preview/` (4, gitignored), `deploy-drop/` (0 html now, see #36), `sources/` (21), `archive/` (15), `lessons-g3/` (2 — real Grade 3 content), `incoming/` top-level (2), `mockup/` (1). Discovered 2026-07-23 while chasing git's "CRLF will be replaced by LF" warning on two review pages: each carried ~3,969 CRLF lines and no guard in the suite noticed. SAME SHAPE AS THE `incoming/` COVERAGE HOLE (`verify-grading-node.js` MIN_LESSONS=100, added after the historical "4 files tested while 105 sat in incoming/" bug) — a guard that reads as comprehensive and silently omits directories. NOTE the guard runs in BOTH `test:fast` and full `test`, and is FIRST in each chain, so it is trusted heavily while covering roughly 1/8 of the .html in the repo. **DO NOT SIMPLY WIDEN THE SCOPE:** at time of logging the `review/` pages already violate the rule, so widening the guard first turns the suite red before anything is fixed. Correct order — fix the emitter (#117), regenerate `review/`, then widen. | E | process | open | 2026-07-23 | — | — |
| 117 | all | — | `tools/make-review.js` | **`make-review.js` bakes local working-copy state into committed artifacts.** It inlines the engine/card CSS+JS bundle into each generated review page verbatim with no normalisation, so a review page inherits whatever line endings the local checkout of the engine files happens to have. Evidence 2026-07-23: `engine/preview-engine.js` (2,896 CRLF lines) and `engine/rao.css` (1,073) had drifted to CRLF in the working tree while the committed blobs were pure LF; 2,896 + 1,073 = 3,969, exactly the CRLF count found in BOTH regenerated review pages. The other seven inlined sources were clean LF. CONSEQUENCE 1 — every regenerated review page shows a whole-file diff instead of the handful of lines that actually changed, making review diffs useless for spotting real changes. CONSEQUENCE 2, THE SERIOUS ONE — **the same command run on two machines produces different committed bytes.** A generated artifact should be a function of its inputs; here it is also a function of the operator's checkout state. FIX: normalise line endings on output in `make-review.js` (emit LF regardless of source state), then regenerate `review/`, and only then widen #116's guard to cover it. WORKAROUND USED 2026-07-23: normalising the two engine files restored them byte-identical to HEAD (md5 match, `git diff --ignore-cr-at-eol` empty, byte counts 226423-2896=223527 and 100877-1073=99804 both verified), and `review/interpret-remainders.html` was regenerated clean (CRLF=0) before the #92 commit `ff2c0d4`. `review/compare_numbers_up_to_five_digits.html` still carries the old mixed CRLF and predates this session. | E | process | open | 2026-07-23 | — | — |
| 118 | all | — | `engine/rao-card.js`, `tools/verify-reset.js` | LAW REVERSAL (a record, not a defect) — the fill-blank typed value is now CLEARED on Try again, reversing the former FR-2 ruling-4 law "the typed value is NEVER cleared — erasing it reads as punishment". Ruled by Venkat 2026-07-23 (#88): a stale wrong answer sitting in the box on retry is friction, not kindness — clear it for a clean slate. THE VALUE STILL SURVIVES THE WRONG CHECK (the child must see the answer they are told is wrong) and clears only on the Try-again tap. Implemented in BRIEF-RETRY-STATE-2 G1: rao-card.js restoreTask() drops the keep[] re-preservation, the LAWS law-3 block is amended and dated, and verify-reset.js A5-through-Try-Again plus the keepValues drill were INVERTED (preservation -> clearing) with dated comments naming this brief. This row exists so the old law is not restored later by someone reading a pre-2026-07-23 note — the exact failure mode that hit the tile-sizing scheme twice. | E | process | closed | 2026-07-23 | 2026-07-23 | Recorded and implemented in one pass (BRIEF-RETRY-STATE-2). Guards: verify-retry-state.js G1 and the inverted verify-reset.js. |
